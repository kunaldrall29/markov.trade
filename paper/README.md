# paper/
One file per calendar day, `YYYY-MM-DD.md`, in the schema of `docs/11-AGENT-SPEC.md` §7. Written by `book-one` with `VENUE=shadow`. Ugly days are kept; days with no run are written as `no run — <reason>`; nothing is backfilled. `PAPER_START_DATE` in `docs/FACTS.md` is the first file here and is never edited.
