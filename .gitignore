# secrets — never committed (docs/14-SECURITY-AND-KEYS.md §5)
*.json.key
keys/*.json
**/secrets/
.env
.env.*
!.env.example

# build output
target/
node_modules/
dist/
.turbo/
.anchor/
test-ledger/

# editor / os
.DS_Store
.idea/
.vscode/
*.swp

# P01 additions
.turbo/
**/.vercel/output/
**/.output/
*.so

# paper tick logs are diagnostics, not the product; the daily .md files are committed
paper/ticks/
