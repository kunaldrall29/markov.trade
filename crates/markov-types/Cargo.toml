[package]
name = "markov-types"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT"
description = "Types shared by the programs and the services: BlockReason, action kinds, policy view, IDs. Must compile for SBF and host."
publish = false

[dependencies]
