[package]
name = "bot"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT"
description = "Telegram bot holding the emergency key: /status, /pause, /revoke, /help."
publish = false

[dependencies]
