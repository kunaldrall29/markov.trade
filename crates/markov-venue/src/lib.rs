//! VenueAdapter trait plus the demo_perps client; the Gate C seam (B11).
//!
//! Scaffold from P01. Empty on purpose; the owning prompt fills it in.
#![forbid(unsafe_code)]
#![warn(clippy::unwrap_used, clippy::expect_used, clippy::panic)]
