[package]
name = "markov-marks"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT"
description = "MarkSource trait: onchain (Pyth devnet account) | house | replay."
publish = false

[dependencies]
base64 = "0.22"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde_json = "1"
thiserror = "2"
