# tests/
- `program/` — LiteSVM, one file per gate (P02). P01's harness proof lives in `programs/markov-mandate/tests/`.
- `integration/` — devnet smoke: fund → act → refuse → revoke → withdraw (P07/P15).
- `e2e/` — Playwright: B10 and the tape beats (P11).
