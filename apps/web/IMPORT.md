# apps/web — provenance

Imported verbatim from `github.com/kunaldrall29/markov-landing-new` at commit `e4e6733` ("Export from Grok", 2026-09-01) on 2026-09-01. This is Kunal's landing page and the Markov Book web app (TanStack Start + Vite + Tailwind v4, routes `/`, `/book`, `/receipts`, `/paper`). Per `docs/09-REPO-STRUCTURE.md` it lives at `apps/web`.

Not imported, and why:

| Path in source repo | Why |
|---|---|
| `.grok/` (builder skills, references, logs) | Grok app-builder tooling, not site code. Only `.grok/app-env.json` is kept because `scripts/with-app-env.mjs` reads it at build time (`VITE_AUTH_ENABLED=false`). |
| `.vercel/output/` | committed build output; regenerated by `npm run build` |
| `artifacts/` | planning + engineering packs moved to `docs/` and `docs/planning/`; generated images/videos/docx/zip left out |
| `screenshots/` | builder QA captures (~15 MB) |
| `AGENTS.md`, `AGENTS.project.md` | builder instructions; the product brief is at `docs/planning/GROK-PROJECT-BRIEF.md` |
| `startup.sh`, `.node_modules.lock` | builder workspace plumbing |

Kept although it is builder scaffolding rather than site code (removal is a P12 decision, not a Session 0 edit): `server/middleware/grok-pwa.ts` and `scripts/grok-pwa-*` (PWA/OG head injection, `/__grok/manifest`), `src/lib/auth/*` + `migrations/auth/` (better-auth on PGLite; dormant with auth off), `src/lib/multiplayer/`, `src/components/preview-host-bridge.tsx`, `public/__grok/`.

`public/videos/keep-the-pile.mp4` is 30 MB and is committed as-is because it is the hero film; moving it to LFS or a CDN is in `docs/BACKLOG.md`.

Build: `npm ci && npm run build` (nitro `vercel` preset → `.vercel/output/`). Preview: `npx vite preview`.
