import { Page } from "@/components/page";

export default function Rule() {
  return (
    <Page title="Invest rule" note="A rule is identified by the on-chain invest mandate version plus schedule. This page is empty until one exists." />
  );
}
