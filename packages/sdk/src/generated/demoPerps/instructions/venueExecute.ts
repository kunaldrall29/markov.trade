/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { DEMO_PERPS_PROGRAM_ADDRESS } from "../programs";

export const VENUE_EXECUTE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  32, 251, 99, 69, 125, 242, 154, 41,
]);

export function getVenueExecuteDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VENUE_EXECUTE_DISCRIMINATOR);
}

export type VenueExecuteInstruction<
  TProgram extends string = typeof DEMO_PERPS_PROGRAM_ADDRESS,
  TAccountMandate extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountMark extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMandate extends string
        ? ReadonlySignerAccount<TAccountMandate> & AccountSignerMeta<TAccountMandate>
        : TAccountMandate,
      TAccountMarket extends string ? ReadonlyAccount<TAccountMarket> : TAccountMarket,
      TAccountMark extends string ? ReadonlyAccount<TAccountMark> : TAccountMark,
      TAccountPosition extends string ? WritableAccount<TAccountPosition> : TAccountPosition,
      ...TRemainingAccounts,
    ]
  >;

export type VenueExecuteInstructionData = {
  discriminator: ReadonlyUint8Array;
  action: number;
  market: ReadonlyUint8Array;
  side: number;
  notional: bigint;
  limitPrice: bigint;
  maxSlippageBps: number;
};

export type VenueExecuteInstructionDataArgs = {
  action: number;
  market: ReadonlyUint8Array;
  side: number;
  notional: number | bigint;
  limitPrice: number | bigint;
  maxSlippageBps: number;
};

export function getVenueExecuteInstructionDataEncoder(): FixedSizeEncoder<VenueExecuteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["action", getU8Encoder()],
      ["market", fixEncoderSize(getBytesEncoder(), 16)],
      ["side", getU8Encoder()],
      ["notional", getU64Encoder()],
      ["limitPrice", getU64Encoder()],
      ["maxSlippageBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VENUE_EXECUTE_DISCRIMINATOR }),
  );
}

export function getVenueExecuteInstructionDataDecoder(): FixedSizeDecoder<VenueExecuteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["action", getU8Decoder()],
    ["market", fixDecoderSize(getBytesDecoder(), 16)],
    ["side", getU8Decoder()],
    ["notional", getU64Decoder()],
    ["limitPrice", getU64Decoder()],
    ["maxSlippageBps", getU16Decoder()],
  ]);
}

export function getVenueExecuteInstructionDataCodec(): FixedSizeCodec<
  VenueExecuteInstructionDataArgs,
  VenueExecuteInstructionData
> {
  return combineCodec(
    getVenueExecuteInstructionDataEncoder(),
    getVenueExecuteInstructionDataDecoder(),
  );
}

export type VenueExecuteInput<
  TAccountMandate extends InstructionSignerInput = InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput = InstructionAccountInput,
  TAccountMark extends InstructionAccountInput = InstructionAccountInput,
  TAccountPosition extends InstructionAccountInput = InstructionAccountInput,
> = {
  /**
   * The mandate PDA, signing through `invoke_signed`. Nothing else can
   * move a position.
   */
  mandate: TAccountMandate;
  market: TAccountMarket;
  mark: TAccountMark;
  position: TAccountPosition;
  action: VenueExecuteInstructionDataArgs["action"];
  marketArg: VenueExecuteInstructionDataArgs["market"];
  side: VenueExecuteInstructionDataArgs["side"];
  notional: VenueExecuteInstructionDataArgs["notional"];
  limitPrice: VenueExecuteInstructionDataArgs["limitPrice"];
  maxSlippageBps: VenueExecuteInstructionDataArgs["maxSlippageBps"];
};

export function getVenueExecuteInstruction<
  TAccountMandate extends InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput,
  TAccountMark extends InstructionAccountInput,
  TAccountPosition extends InstructionAccountInput,
  TProgramAddress extends Address = typeof DEMO_PERPS_PROGRAM_ADDRESS,
>(
  input: VenueExecuteInput<TAccountMandate, TAccountMarket, TAccountMark, TAccountPosition>,
  config?: { programAddress?: TProgramAddress },
): VenueExecuteInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountMandate, InstructionAccountInputAddress<TAccountMandate>>,
  ResolvedInstructionAccountMeta<TAccountMarket, InstructionAccountInputAddress<TAccountMarket>>,
  ResolvedInstructionAccountMeta<TAccountMark, InstructionAccountInputAddress<TAccountMark>>,
  ResolvedInstructionAccountMeta<TAccountPosition, InstructionAccountInputAddress<TAccountPosition>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEMO_PERPS_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    mandate: { value: input.mandate ?? null, isSigner: true, isWritable: false },
    market: { value: input.market ?? null, isSigner: false, isWritable: false },
    mark: { value: input.mark ?? null, isSigner: false, isWritable: false },
    position: { value: input.position ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input, market: input.marketArg };

  return Object.freeze({
    accounts: [
      getAccountMeta("mandate", accounts.mandate),
      getAccountMeta("market", accounts.market),
      getAccountMeta("mark", accounts.mark),
      getAccountMeta("position", accounts.position),
    ],
    data: getVenueExecuteInstructionDataEncoder().encode(args as VenueExecuteInstructionDataArgs),
    programAddress,
  } as VenueExecuteInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountMandate,
      InstructionAccountInputAddress<TAccountMandate>
    >,
    ResolvedInstructionAccountMeta<TAccountMarket, InstructionAccountInputAddress<TAccountMarket>>,
    ResolvedInstructionAccountMeta<TAccountMark, InstructionAccountInputAddress<TAccountMark>>,
    ResolvedInstructionAccountMeta<
      TAccountPosition,
      InstructionAccountInputAddress<TAccountPosition>
    >
  >);
}

export type ParsedVenueExecuteInstruction<
  TProgram extends string = typeof DEMO_PERPS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The mandate PDA, signing through `invoke_signed`. Nothing else can
     * move a position.
     */
    mandate: TAccountMetas[0];
    market: TAccountMetas[1];
    mark: TAccountMetas[2];
    position: TAccountMetas[3];
  };
  data: VenueExecuteInstructionData;
};

export function parseVenueExecuteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVenueExecuteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      mandate: getNextAccount(),
      market: getNextAccount(),
      mark: getNextAccount(),
      position: getNextAccount(),
    },
    data: getVenueExecuteInstructionDataDecoder().decode(instruction.data),
  };
}
