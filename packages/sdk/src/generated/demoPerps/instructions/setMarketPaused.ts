/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { DEMO_PERPS_PROGRAM_ADDRESS } from "../programs";

export const SET_MARKET_PAUSED_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  233, 31, 161, 248, 178, 111, 102, 65,
]);

export function getSetMarketPausedDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_MARKET_PAUSED_DISCRIMINATOR);
}

export type SetMarketPausedInstruction<
  TProgram extends string = typeof DEMO_PERPS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMarket extends string ? WritableAccount<TAccountMarket> : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type SetMarketPausedInstructionData = { discriminator: ReadonlyUint8Array; paused: boolean };

export type SetMarketPausedInstructionDataArgs = { paused: boolean };

export function getSetMarketPausedInstructionDataEncoder(): FixedSizeEncoder<SetMarketPausedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["paused", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_MARKET_PAUSED_DISCRIMINATOR }),
  );
}

export function getSetMarketPausedInstructionDataDecoder(): FixedSizeDecoder<SetMarketPausedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["paused", getBooleanDecoder()],
  ]);
}

export function getSetMarketPausedInstructionDataCodec(): FixedSizeCodec<
  SetMarketPausedInstructionDataArgs,
  SetMarketPausedInstructionData
> {
  return combineCodec(
    getSetMarketPausedInstructionDataEncoder(),
    getSetMarketPausedInstructionDataDecoder(),
  );
}

export type SetMarketPausedInput<
  TAccountAuthority extends InstructionSignerInput = InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput = InstructionAccountInput,
> = {
  authority: TAccountAuthority;
  market: TAccountMarket;
  paused: SetMarketPausedInstructionDataArgs["paused"];
};

export function getSetMarketPausedInstruction<
  TAccountAuthority extends InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput,
  TProgramAddress extends Address = typeof DEMO_PERPS_PROGRAM_ADDRESS,
>(
  input: SetMarketPausedInput<TAccountAuthority, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): SetMarketPausedInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountAuthority,
    InstructionAccountInputAddress<TAccountAuthority>
  >,
  ResolvedInstructionAccountMeta<TAccountMarket, InstructionAccountInputAddress<TAccountMarket>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEMO_PERPS_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isSigner: true, isWritable: false },
    market: { value: input.market ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("market", accounts.market),
    ],
    data: getSetMarketPausedInstructionDataEncoder().encode(
      args as SetMarketPausedInstructionDataArgs,
    ),
    programAddress,
  } as SetMarketPausedInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountAuthority,
      InstructionAccountInputAddress<TAccountAuthority>
    >,
    ResolvedInstructionAccountMeta<TAccountMarket, InstructionAccountInputAddress<TAccountMarket>>
  >);
}

export type ParsedSetMarketPausedInstruction<
  TProgram extends string = typeof DEMO_PERPS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    market: TAccountMetas[1];
  };
  data: SetMarketPausedInstructionData;
};

export function parseSetMarketPausedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMarketPausedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), market: getNextAccount() },
    data: getSetMarketPausedInstructionDataDecoder().decode(instruction.data),
  };
}
