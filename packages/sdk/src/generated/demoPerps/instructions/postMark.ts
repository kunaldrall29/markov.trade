/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI32Decoder,
  getI32Encoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { DEMO_PERPS_PROGRAM_ADDRESS } from "../programs";

export const POST_MARK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  224, 48, 77, 105, 2, 74, 42, 145,
]);

export function getPostMarkDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(POST_MARK_DISCRIMINATOR);
}

export type PostMarkInstruction<
  TProgram extends string = typeof DEMO_PERPS_PROGRAM_ADDRESS,
  TAccountPoster extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountMark extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPoster extends string
        ? ReadonlySignerAccount<TAccountPoster> & AccountSignerMeta<TAccountPoster>
        : TAccountPoster,
      TAccountMarket extends string ? ReadonlyAccount<TAccountMarket> : TAccountMarket,
      TAccountMark extends string ? WritableAccount<TAccountMark> : TAccountMark,
      ...TRemainingAccounts,
    ]
  >;

export type PostMarkInstructionData = {
  discriminator: ReadonlyUint8Array;
  price: bigint;
  expo: number;
  publishTime: bigint;
};

export type PostMarkInstructionDataArgs = {
  price: number | bigint;
  expo: number;
  publishTime: number | bigint;
};

export function getPostMarkInstructionDataEncoder(): FixedSizeEncoder<PostMarkInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["price", getI64Encoder()],
      ["expo", getI32Encoder()],
      ["publishTime", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: POST_MARK_DISCRIMINATOR }),
  );
}

export function getPostMarkInstructionDataDecoder(): FixedSizeDecoder<PostMarkInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["price", getI64Decoder()],
    ["expo", getI32Decoder()],
    ["publishTime", getI64Decoder()],
  ]);
}

export function getPostMarkInstructionDataCodec(): FixedSizeCodec<
  PostMarkInstructionDataArgs,
  PostMarkInstructionData
> {
  return combineCodec(getPostMarkInstructionDataEncoder(), getPostMarkInstructionDataDecoder());
}

export type PostMarkInput<
  TAccountPoster extends InstructionSignerInput = InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput = InstructionAccountInput,
  TAccountMark extends InstructionAccountInput = InstructionAccountInput,
> = {
  poster: TAccountPoster;
  market: TAccountMarket;
  mark: TAccountMark;
  price: PostMarkInstructionDataArgs["price"];
  expo: PostMarkInstructionDataArgs["expo"];
  publishTime: PostMarkInstructionDataArgs["publishTime"];
};

export function getPostMarkInstruction<
  TAccountPoster extends InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput,
  TAccountMark extends InstructionAccountInput,
  TProgramAddress extends Address = typeof DEMO_PERPS_PROGRAM_ADDRESS,
>(
  input: PostMarkInput<TAccountPoster, TAccountMarket, TAccountMark>,
  config?: { programAddress?: TProgramAddress },
): PostMarkInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountPoster, InstructionAccountInputAddress<TAccountPoster>>,
  ResolvedInstructionAccountMeta<TAccountMarket, InstructionAccountInputAddress<TAccountMarket>>,
  ResolvedInstructionAccountMeta<TAccountMark, InstructionAccountInputAddress<TAccountMark>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEMO_PERPS_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    poster: { value: input.poster ?? null, isSigner: true, isWritable: false },
    market: { value: input.market ?? null, isSigner: false, isWritable: false },
    mark: { value: input.mark ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("poster", accounts.poster),
      getAccountMeta("market", accounts.market),
      getAccountMeta("mark", accounts.mark),
    ],
    data: getPostMarkInstructionDataEncoder().encode(args as PostMarkInstructionDataArgs),
    programAddress,
  } as PostMarkInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountPoster, InstructionAccountInputAddress<TAccountPoster>>,
    ResolvedInstructionAccountMeta<TAccountMarket, InstructionAccountInputAddress<TAccountMarket>>,
    ResolvedInstructionAccountMeta<TAccountMark, InstructionAccountInputAddress<TAccountMark>>
  >);
}

export type ParsedPostMarkInstruction<
  TProgram extends string = typeof DEMO_PERPS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    poster: TAccountMetas[0];
    market: TAccountMetas[1];
    mark: TAccountMetas[2];
  };
  data: PostMarkInstructionData;
};

export function parsePostMarkInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPostMarkInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { poster: getNextAccount(), market: getNextAccount(), mark: getNextAccount() },
    data: getPostMarkInstructionDataDecoder().decode(instruction.data),
  };
}
