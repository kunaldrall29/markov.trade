/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getMandateStateDecoder,
  getMandateStateEncoder,
  getPolicyDecoder,
  getPolicyEncoder,
  type MandateState,
  type MandateStateArgs,
  type Policy,
  type PolicyArgs,
} from "../types";

export const MANDATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  113, 216, 98, 159, 185, 63, 55, 18,
]);

export function getMandateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MANDATE_DISCRIMINATOR);
}

export type Mandate = {
  discriminator: ReadonlyUint8Array;
  /** The only key that can withdraw, unpause or amend. */
  owner: Address;
  /** The house agent. Propose-only. */
  operator: Address;
  /** Pause and revoke only. Never unpause, never withdraw. */
  emergency: Address;
  /** `BOOK_ONE` for the house book. */
  strategyId: ReadonlyUint8Array;
  state: MandateState;
  policy: Policy;
  /** Token account owned by this PDA. */
  vault: Address;
  /** Settlement mint (USDC-d). */
  mint: Address;
  /**
   * The mark account the policy binds this mandate to (a Pyth
   * `PriceUpdateV2`); the freshness gate refuses any other account.
   */
  markAccount: Address;
  /** SOL/USD feed id the mark must carry. */
  feedId: ReadonlyUint8Array;
  /** UTC day index (`unix_timestamp.div_euclid(86_400)`) for the rolling counters. */
  dayEpoch: bigint;
  dayNotionalUsed: bigint;
  daySpendUsed: bigint;
  /** Monotonic; goes on every receipt. */
  actionSeq: bigint;
  /** Ring of recent intent ids for replay protection; cleared on day rollover. */
  recentIntents: Array<ReadonlyUint8Array>;
  recentIntentsLen: number;
  recentIntentsNext: number;
  createdAt: bigint;
  /** PDA seed nonce, so one owner can hold several mandates for one strategy. */
  nonce: bigint;
  bump: number;
  vaultBump: number;
  /**
   * Room for the Phase-1 fields (`max_net_delta_usd`, `max_gross_usd` on
   * chain) without a migration (docs/10 §1.1).
   */
  reserve: ReadonlyUint8Array;
};

export type MandateArgs = {
  /** The only key that can withdraw, unpause or amend. */
  owner: Address;
  /** The house agent. Propose-only. */
  operator: Address;
  /** Pause and revoke only. Never unpause, never withdraw. */
  emergency: Address;
  /** `BOOK_ONE` for the house book. */
  strategyId: ReadonlyUint8Array;
  state: MandateStateArgs;
  policy: PolicyArgs;
  /** Token account owned by this PDA. */
  vault: Address;
  /** Settlement mint (USDC-d). */
  mint: Address;
  /**
   * The mark account the policy binds this mandate to (a Pyth
   * `PriceUpdateV2`); the freshness gate refuses any other account.
   */
  markAccount: Address;
  /** SOL/USD feed id the mark must carry. */
  feedId: ReadonlyUint8Array;
  /** UTC day index (`unix_timestamp.div_euclid(86_400)`) for the rolling counters. */
  dayEpoch: number | bigint;
  dayNotionalUsed: number | bigint;
  daySpendUsed: number | bigint;
  /** Monotonic; goes on every receipt. */
  actionSeq: number | bigint;
  /** Ring of recent intent ids for replay protection; cleared on day rollover. */
  recentIntents: Array<ReadonlyUint8Array>;
  recentIntentsLen: number;
  recentIntentsNext: number;
  createdAt: number | bigint;
  /** PDA seed nonce, so one owner can hold several mandates for one strategy. */
  nonce: number | bigint;
  bump: number;
  vaultBump: number;
  /**
   * Room for the Phase-1 fields (`max_net_delta_usd`, `max_gross_usd` on
   * chain) without a migration (docs/10 §1.1).
   */
  reserve: ReadonlyUint8Array;
};

/** Gets the encoder for {@link MandateArgs} account data. */
export function getMandateEncoder(): FixedSizeEncoder<MandateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["operator", getAddressEncoder()],
      ["emergency", getAddressEncoder()],
      ["strategyId", fixEncoderSize(getBytesEncoder(), 16)],
      ["state", getMandateStateEncoder()],
      ["policy", getPolicyEncoder()],
      ["vault", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["markAccount", getAddressEncoder()],
      ["feedId", fixEncoderSize(getBytesEncoder(), 32)],
      ["dayEpoch", getI64Encoder()],
      ["dayNotionalUsed", getU64Encoder()],
      ["daySpendUsed", getU64Encoder()],
      ["actionSeq", getU64Encoder()],
      ["recentIntents", getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32), { size: 8 })],
      ["recentIntentsLen", getU8Encoder()],
      ["recentIntentsNext", getU8Encoder()],
      ["createdAt", getI64Encoder()],
      ["nonce", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["vaultBump", getU8Encoder()],
      ["reserve", fixEncoderSize(getBytesEncoder(), 128)],
    ]),
    (value) => ({ ...value, discriminator: MANDATE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Mandate} account data. */
export function getMandateDecoder(): FixedSizeDecoder<Mandate> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["operator", getAddressDecoder()],
    ["emergency", getAddressDecoder()],
    ["strategyId", fixDecoderSize(getBytesDecoder(), 16)],
    ["state", getMandateStateDecoder()],
    ["policy", getPolicyDecoder()],
    ["vault", getAddressDecoder()],
    ["mint", getAddressDecoder()],
    ["markAccount", getAddressDecoder()],
    ["feedId", fixDecoderSize(getBytesDecoder(), 32)],
    ["dayEpoch", getI64Decoder()],
    ["dayNotionalUsed", getU64Decoder()],
    ["daySpendUsed", getU64Decoder()],
    ["actionSeq", getU64Decoder()],
    ["recentIntents", getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32), { size: 8 })],
    ["recentIntentsLen", getU8Decoder()],
    ["recentIntentsNext", getU8Decoder()],
    ["createdAt", getI64Decoder()],
    ["nonce", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["vaultBump", getU8Decoder()],
    ["reserve", fixDecoderSize(getBytesDecoder(), 128)],
  ]);
}

/** Gets the codec for {@link Mandate} account data. */
export function getMandateCodec(): FixedSizeCodec<MandateArgs, Mandate> {
  return combineCodec(getMandateEncoder(), getMandateDecoder());
}

export function decodeMandate<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Mandate, TAddress>;
export function decodeMandate<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Mandate, TAddress>;
export function decodeMandate<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Mandate, TAddress> | MaybeAccount<Mandate, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getMandateDecoder());
}

export async function fetchMandate<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Mandate, TAddress>> {
  const maybeAccount = await fetchMaybeMandate(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMandate<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Mandate, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMandate(maybeAccount);
}

export async function fetchAllMandate(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Mandate>[]> {
  const maybeAccounts = await fetchAllMaybeMandate(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMandate(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Mandate>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeMandate(maybeAccount));
}

export function getMandateSize(): number {
  return 995;
}
