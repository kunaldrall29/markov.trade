/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getBlockReasonDecoder,
  getBlockReasonEncoder,
  type BlockReason,
  type BlockReasonArgs,
} from "../types";

export const REFUSAL_RECEIPT_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  230, 141, 15, 68, 48, 101, 19, 118,
]);

export function getRefusalReceiptEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REFUSAL_RECEIPT_EVENT_DISCRIMINATOR);
}

export type RefusalReceiptEvent = {
  seq: bigint;
  intentId: ReadonlyUint8Array;
  mandate: Address;
  operator: Address;
  strategyId: ReadonlyUint8Array;
  venue: Address;
  action: number;
  notional: bigint;
  /** `BlockReason` discriminant; 0–10 are the eleven the predecessor emitted. */
  reason: BlockReason;
  /** Which rung of the ladder refused, 1-based, matching docs/10 §3. */
  gateIndex: number;
  forced: boolean;
  ts: bigint;
  slot: bigint;
};

export type RefusalReceiptEventArgs = {
  seq: number | bigint;
  intentId: ReadonlyUint8Array;
  mandate: Address;
  operator: Address;
  strategyId: ReadonlyUint8Array;
  venue: Address;
  action: number;
  notional: number | bigint;
  /** `BlockReason` discriminant; 0–10 are the eleven the predecessor emitted. */
  reason: BlockReasonArgs;
  /** Which rung of the ladder refused, 1-based, matching docs/10 §3. */
  gateIndex: number;
  forced: boolean;
  ts: number | bigint;
  slot: number | bigint;
};

/** Gets the encoder for {@link RefusalReceiptEventArgs} event data. */
export function getRefusalReceiptEventEncoder(): FixedSizeEncoder<RefusalReceiptEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["seq", getU64Encoder()],
      ["intentId", fixEncoderSize(getBytesEncoder(), 32)],
      ["mandate", getAddressEncoder()],
      ["operator", getAddressEncoder()],
      ["strategyId", fixEncoderSize(getBytesEncoder(), 16)],
      ["venue", getAddressEncoder()],
      ["action", getU8Encoder()],
      ["notional", getU64Encoder()],
      ["reason", getBlockReasonEncoder()],
      ["gateIndex", getU8Encoder()],
      ["forced", getBooleanEncoder()],
      ["ts", getI64Encoder()],
      ["slot", getU64Encoder()],
    ]),
    [getConstantEncoder(REFUSAL_RECEIPT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link RefusalReceiptEvent} event data. */
export function getRefusalReceiptEventDecoder(): FixedSizeDecoder<RefusalReceiptEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["seq", getU64Decoder()],
      ["intentId", fixDecoderSize(getBytesDecoder(), 32)],
      ["mandate", getAddressDecoder()],
      ["operator", getAddressDecoder()],
      ["strategyId", fixDecoderSize(getBytesDecoder(), 16)],
      ["venue", getAddressDecoder()],
      ["action", getU8Decoder()],
      ["notional", getU64Decoder()],
      ["reason", getBlockReasonDecoder()],
      ["gateIndex", getU8Decoder()],
      ["forced", getBooleanDecoder()],
      ["ts", getI64Decoder()],
      ["slot", getU64Decoder()],
    ]),
    [getConstantDecoder(REFUSAL_RECEIPT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link RefusalReceiptEvent} event data. */
export function getRefusalReceiptEventCodec(): FixedSizeCodec<
  RefusalReceiptEventArgs,
  RefusalReceiptEvent
> {
  return combineCodec(getRefusalReceiptEventEncoder(), getRefusalReceiptEventDecoder());
}

/** Parses the provided bytes into {@link RefusalReceiptEvent} event data. */
export function parseRefusalReceiptEvent(
  data: ReadonlyUint8Array | Uint8Array,
): RefusalReceiptEvent {
  if (containsBytes(data, REFUSAL_RECEIPT_EVENT_DISCRIMINATOR, 0)) {
    return getRefusalReceiptEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "RefusalReceiptEvent" event discriminators.',
  );
}
