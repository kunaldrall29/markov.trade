/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

export type Policy = {
  venues: Array<Address>;
  venuesLen: number;
  tokens: Array<Address>;
  tokensLen: number;
  /** Bitmask over `action_bits`. */
  allowedActions: number;
  /** Notional per action, in mint base units. */
  perTxCap: bigint;
  /** Notional per UTC day. */
  dailyCap: bigint;
  /** Data/compute spend per call and per UTC day. */
  spendPerCall: bigint;
  spendDaily: bigint;
  maxSlippageBps: number;
  /**
   * Freshness in **seconds** since the mark's `publish_time` (ADR-003:
   * seconds, not slots — devnet pacing is ≈165 ms/slot and moves).
   */
  maxMarkAgeSecs: bigint;
  /**
   * Unix seconds. The expiry gate reads this; `amend_policy` may only
   * shorten it. (docs/10 §1.2 puts expiry in the policy; the mandate keeps
   * `created_at` only, so there is one source of truth.)
   */
  expiryTs: bigint;
};

export type PolicyArgs = {
  venues: Array<Address>;
  venuesLen: number;
  tokens: Array<Address>;
  tokensLen: number;
  /** Bitmask over `action_bits`. */
  allowedActions: number;
  /** Notional per action, in mint base units. */
  perTxCap: number | bigint;
  /** Notional per UTC day. */
  dailyCap: number | bigint;
  /** Data/compute spend per call and per UTC day. */
  spendPerCall: number | bigint;
  spendDaily: number | bigint;
  maxSlippageBps: number;
  /**
   * Freshness in **seconds** since the mark's `publish_time` (ADR-003:
   * seconds, not slots — devnet pacing is ≈165 ms/slot and moves).
   */
  maxMarkAgeSecs: number | bigint;
  /**
   * Unix seconds. The expiry gate reads this; `amend_policy` may only
   * shorten it. (docs/10 §1.2 puts expiry in the policy; the mandate keeps
   * `created_at` only, so there is one source of truth.)
   */
  expiryTs: number | bigint;
};

export function getPolicyEncoder(): FixedSizeEncoder<PolicyArgs> {
  return getStructEncoder([
    ["venues", getArrayEncoder(getAddressEncoder(), { size: 4 })],
    ["venuesLen", getU8Encoder()],
    ["tokens", getArrayEncoder(getAddressEncoder(), { size: 4 })],
    ["tokensLen", getU8Encoder()],
    ["allowedActions", getU16Encoder()],
    ["perTxCap", getU64Encoder()],
    ["dailyCap", getU64Encoder()],
    ["spendPerCall", getU64Encoder()],
    ["spendDaily", getU64Encoder()],
    ["maxSlippageBps", getU16Encoder()],
    ["maxMarkAgeSecs", getU64Encoder()],
    ["expiryTs", getI64Encoder()],
  ]);
}

export function getPolicyDecoder(): FixedSizeDecoder<Policy> {
  return getStructDecoder([
    ["venues", getArrayDecoder(getAddressDecoder(), { size: 4 })],
    ["venuesLen", getU8Decoder()],
    ["tokens", getArrayDecoder(getAddressDecoder(), { size: 4 })],
    ["tokensLen", getU8Decoder()],
    ["allowedActions", getU16Decoder()],
    ["perTxCap", getU64Decoder()],
    ["dailyCap", getU64Decoder()],
    ["spendPerCall", getU64Decoder()],
    ["spendDaily", getU64Decoder()],
    ["maxSlippageBps", getU16Decoder()],
    ["maxMarkAgeSecs", getU64Decoder()],
    ["expiryTs", getI64Decoder()],
  ]);
}

export function getPolicyCodec(): FixedSizeCodec<PolicyArgs, Policy> {
  return combineCodec(getPolicyEncoder(), getPolicyDecoder());
}
