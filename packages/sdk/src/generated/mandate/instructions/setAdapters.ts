/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findRegistryPda } from "../pdas";
import { MARKOV_MANDATE_PROGRAM_ADDRESS } from "../programs";

export const SET_ADAPTERS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  172, 129, 83, 115, 16, 220, 84, 119,
]);

export function getSetAdaptersDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_ADAPTERS_DISCRIMINATOR);
}

export type SetAdaptersInstruction<
  TProgram extends string = typeof MARKOV_MANDATE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountRegistry extends string ? WritableAccount<TAccountRegistry> : TAccountRegistry,
      ...TRemainingAccounts,
    ]
  >;

export type SetAdaptersInstructionData = {
  discriminator: ReadonlyUint8Array;
  adapters: Array<Address>;
};

export type SetAdaptersInstructionDataArgs = { adapters: Array<Address> };

export function getSetAdaptersInstructionDataEncoder(): Encoder<SetAdaptersInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["adapters", getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: SET_ADAPTERS_DISCRIMINATOR }),
  );
}

export function getSetAdaptersInstructionDataDecoder(): Decoder<SetAdaptersInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["adapters", getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getSetAdaptersInstructionDataCodec(): Codec<
  SetAdaptersInstructionDataArgs,
  SetAdaptersInstructionData
> {
  return combineCodec(
    getSetAdaptersInstructionDataEncoder(),
    getSetAdaptersInstructionDataDecoder(),
  );
}

export type SetAdaptersAsyncInput<
  TAccountAdmin extends InstructionSignerInput = InstructionSignerInput,
  TAccountRegistry extends InstructionAccountInput = InstructionAccountInput,
> = {
  admin: TAccountAdmin;
  registry?: TAccountRegistry;
  adapters: SetAdaptersInstructionDataArgs["adapters"];
};

export async function getSetAdaptersInstructionAsync<
  TAccountAdmin extends InstructionSignerInput,
  TAccountRegistry extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARKOV_MANDATE_PROGRAM_ADDRESS,
>(
  input: SetAdaptersAsyncInput<TAccountAdmin, TAccountRegistry>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetAdaptersInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountAdmin, InstructionAccountInputAddress<TAccountAdmin>>,
    ResolvedInstructionAccountMeta<
      TAccountRegistry,
      InstructionAccountInputAddress<TAccountRegistry>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARKOV_MANDATE_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isSigner: true, isWritable: false },
    registry: { value: input.registry ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.registry.value) {
    accounts.registry.value = await findRegistryPda({ programAddress });
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("registry", accounts.registry),
    ],
    data: getSetAdaptersInstructionDataEncoder().encode(args as SetAdaptersInstructionDataArgs),
    programAddress,
  } as SetAdaptersInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountAdmin, InstructionAccountInputAddress<TAccountAdmin>>,
    ResolvedInstructionAccountMeta<
      TAccountRegistry,
      InstructionAccountInputAddress<TAccountRegistry>
    >
  >);
}

export type SetAdaptersInput<
  TAccountAdmin extends InstructionSignerInput = InstructionSignerInput,
  TAccountRegistry extends InstructionAccountInput = InstructionAccountInput,
> = {
  admin: TAccountAdmin;
  registry: TAccountRegistry;
  adapters: SetAdaptersInstructionDataArgs["adapters"];
};

export function getSetAdaptersInstruction<
  TAccountAdmin extends InstructionSignerInput,
  TAccountRegistry extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARKOV_MANDATE_PROGRAM_ADDRESS,
>(
  input: SetAdaptersInput<TAccountAdmin, TAccountRegistry>,
  config?: { programAddress?: TProgramAddress },
): SetAdaptersInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountAdmin, InstructionAccountInputAddress<TAccountAdmin>>,
  ResolvedInstructionAccountMeta<TAccountRegistry, InstructionAccountInputAddress<TAccountRegistry>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARKOV_MANDATE_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isSigner: true, isWritable: false },
    registry: { value: input.registry ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("registry", accounts.registry),
    ],
    data: getSetAdaptersInstructionDataEncoder().encode(args as SetAdaptersInstructionDataArgs),
    programAddress,
  } as SetAdaptersInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountAdmin, InstructionAccountInputAddress<TAccountAdmin>>,
    ResolvedInstructionAccountMeta<
      TAccountRegistry,
      InstructionAccountInputAddress<TAccountRegistry>
    >
  >);
}

export type ParsedSetAdaptersInstruction<
  TProgram extends string = typeof MARKOV_MANDATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    registry: TAccountMetas[1];
  };
  data: SetAdaptersInstructionData;
};

export function parseSetAdaptersInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetAdaptersInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), registry: getNextAccount() },
    data: getSetAdaptersInstructionDataDecoder().decode(instruction.data),
  };
}
