# Global pause (devnet)
# solana-keygen pubkey keys/deployer.json
# (after deploy) send set_global_pause via the admin instruction.

# Keeper halt: SIGTERM the keeper replica. Leader lock is Redis in production.

# Reconcile mismatch: freeze invest automation for the account; rebuild positions from venue APIs; do not rewrite receipts.
