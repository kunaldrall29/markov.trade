{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": [".vercel/output/**", ".output/**", "dist/**"] },
    "typecheck": { "dependsOn": ["^build"] },
    "lint": {}
  }
}
