# FACTS — Markov Book
Last full verification: 2026-09-01 (13-agent re-verification)   Decisions D0–D4 accepted: 2026-09-02   Verified by: Session 0 / Session 1

Rules (from `docs/17-FACTS-TEMPLATE.md`): every row has a value, a date and a source. `PENDING` blocks work that depends on it. Nothing below is guessed; where a value comes from someone else's ledger it says so and says whether this session re-verified it.

Two deployed `markov-mandate`-shaped programs exist on devnet. This file names **`5o8E…`** as `PROGRAM_ID` because it is the one that has emitted all eleven `BlockReason`s on chain, and records **`CT2n…`** as its dormant predecessor. Whether Gate B upgrades `5o8E…` in place or deploys a successor is decision **D3** (`docs/adr/ADR-004`), still open.

## Cluster and toolchain (this workstation, 2026-09-01)
| Key | Value | Verified | Source |
|---|---|---|---|
| CLUSTER | devnet (`https://api.devnet.solana.com`, cluster version `4.3.0-beta.3`, slot ≈ 491,626,706, epoch 1138) | 2026-09-01 | `solana config set --url devnet`; `solana cluster-version`; `solana epoch-info` |
| ANCHOR_VERSION | **installed: anchor-cli 0.31.1 (active) and 1.1.2**, both via avm 1.1.2 (`avm use 0.31.1`). Both deployed programs pin `anchor_version = "0.31.1"`. Current stable on crates.io is **1.1.2** (2026-06-26); `2.0.0-rc.1` (2026-08-12). **Decided 2026-09-02 (ADR-001): pin `anchor_version = "1.1.2"`.** | 2026-09-01 / 02 | `avm install 0.31.1 && anchor --version`; `https://crates.io/api/v1/crates/anchor-lang`; `gh api repos/coral-xyz/anchor/releases`; `Anchor.toml` in both program repos |
| SOLANA_CLI_VERSION | **Installed: Agave 4.2.2** (src c9c6f328 via `agave-install init 4.2.2`; the `stable` install e29e5d91 is the same version), with solana-keygen 4.2.2, cargo-build-sbf 4.1.0, platform-tools v1.54. Agave GitHub latest stable v4.2.2 (2026-08-28). Releases present under `~/.local/share/solana/install/releases`: `4.2.2`, `stable-e29e5d9…`, **`2.1.0` and `3.1.10` — pulled in by anchor-cli 0.31.1 and 1.1.2 respectively.** **The active release is volatile: any `anchor` invocation re-initialises `active_release` to that Anchor version's default Solana (0.31.1 → 2.1.0, 1.1.2 → 3.1.10) unless `Anchor.toml [toolchain] solana_version` pins it.** Observed three times today, the third caused by the verification agents' own `anchor --version` calls; the verification pass found the release at 2.1.0 while this row said 4.2.2 (the refuter's symlink timestamps show the earlier "restored" note was wrong or immediately undone). Re-initialised to 4.2.2 at the end of Session 0 (`readlink` → `releases/4.2.2`). **Until P01 pins `solana_version`, run `agave-install init 4.2.2` before trusting `solana` on PATH, and never run `anchor` outside the project.** **Decided 2026-09-02 (ADR-001): pin `solana_version = "4.2.2"`.** | 2026-09-01 / 02 | `solana --version` and `readlink -f ~/.local/share/solana/install/active_release` before/after `anchor --version`; verification pass 2; `gh api repos/anza-xyz/agave/releases` |
| RUST_VERSION | host rustc **1.97.1** (rustup `stable`, only toolchain installed). The `5o8E…` program declares `rust-version = "1.79"` (`programs/mandate/Cargo.toml`); its authoring box used rustc 1.85.0 per upstream `kunaldrall29/markov` `docs/FACTS.md` ("rustc (host) 1.85.0, 2026-08-24") and its CI pins `dtolnay/rust-toolchain@1.85.0`. `rust-toolchain.toml` for this repo: PENDING (P01). | 2026-09-01 | `rustc --version`; `rustup toolchain list`; upstream FACTS and `.github/workflows/ci.yml` |
| LITESVM_VERSION | **0.16.0 — proven**: the `ping_lands_on_litesvm` tests load the built `.so` into LiteSVM 0.16.0 next to anchor-lang 1.1.2 and pass (dev-deps `solana-message 4`, `solana-transaction 4`, `solana-signer 3`, `solana-keypair 3`). Earlier note kept for the record: Latest **0.16.0** (2026-08-24) depends on `agave-*`/`solana-*` 4.2/4.3 crates; anchor-lang 0.31.1 depends on `solana-program ^2`; anchor-lang 1.1.2 has no `solana-program` dep but ~20 component crates (`solana-pubkey ^3`, `solana-instruction ^3`, …) and `solana-pubkey 3.x` re-exports `solana-address 2.x`, so there is no Pubkey type clash with LiteSVM 0.16. **Host `cargo check` (verification agent, 2026-09-01):** `anchor-lang 1.1.2 + litesvm 0.16.0 + pyth-solana-receiver-sdk 2.0.0` resolves to one graph and **passes**; `anchor-lang 0.31.1 + litesvm 0.6.1 + pyth-solana-receiver-sdk 1.0.1` resolves but **fails** (E0277 `PriceFeedMessage: BorshSerialize`, because `pythnet-sdk 2.3.1`'s open `anchor-lang >=0.28` bound pulls 1.1.2/borsh 1 next to 0.31.1/borsh 0.10) unless the lockfile is forced with `cargo update --precise`. Anchor 1.1.2's own project template pins `litesvm = "0.10.0"`; `anchor-litesvm 0.4.0` pins `litesvm ^0.11`. **SBF build + LiteSVM test run still not done** (P01 pre-flight). | 2026-09-01 | crates.io dependency API; `cargo check` runs recorded in the verification workflow (`wf_f6e45809-31a`, judge:protocol) |
| SURFPOOL_VERSION | not installed; latest v1.5.0 (2026-07-13). Not needed for a devnet-only mock-venue build unless LiteSVM proves incompatible. | 2026-09-01 | `gh api repos/txtx/surfpool/releases` |
| CODAMA_VERSION | PENDING pin. Latest: `codama` 1.10.2, `@codama/nodes-from-anchor` 1.5.5, `@codama/renderers-js` 2.4.0. | 2026-09-01 | `npm view <pkg> version` |
| KIT_VERSION | PENDING pin. Latest: `@solana/kit` 8.2.0, `@solana/kit-plugin-wallet` 0.19.0, `@solana/react` 8.2.0, `@solana/wallet-standard` 1.1.6. Legacy `@solana/web3.js` is 1.98.4 (the existing TS stack uses it; not for new code). | 2026-09-01 | `npm view <pkg> version` |
| ANCHOR_TS_CLIENT | `@coral-xyz/anchor` npm latest is **0.32.1** (last published 2025-10-10). `@anchor-lang/core` 1.1.2 exists. Not needed if the web client is Codama-generated. | 2026-09-01 | `npm view @coral-xyz/anchor dist-tags time.modified`; `npm view @anchor-lang/core version` |
| WEB_STACK_LATEST | `@tanstack/react-start` 1.168.49, `@tanstack/react-router` 1.170.32, `tailwindcss` 4.3.3, `@tailwindcss/vite` 4.3.3, `vite` 8.2.2, `react` 19.2.8, `lucide-react` 1.39.0, `zustand` 5.0.15, `@playwright/test` 1.62.1. Pins PENDING (P01). | 2026-09-01 | `npm view <pkg> version` |
| NODE / PNPM / BUN | node v24.19.0, pnpm 9.15.9, npm 11.17.0, bun 1.4.0 (`~/.bun/bin`), git 2.43.0, docker 29.7.2 | 2026-09-01 | `node --version` etc. |
| FAUCET | `solana airdrop 1` and `0.5` to a throwaway key both failed: "rate limit reached". P01 pre-flight #2 not yet satisfied from this IP. | 2026-09-01 | `solana airdrop` ×2 |

## Programs and accounts
| Key | Value | Verified | Source |
|---|---|---|---|
| PROGRAM_ID | **`5o8EAwdHyQ31Nmt6tUDm1y6PNDt5STmVvA6CX3E6WJPm`** — executable, BPF upgradeable loader, ProgramData `8313nq75PfJ1BE2Z2i5JWXjG5iJXRRRXcZJXz2kH1K85`, last deployed slot **489249058**, data length 405120. Source: `github.com/kunaldrall29/markov` `programs/mandate` (`declare_id!` matches; Anchor 0.31.1). 98 signatures 2026-08-27T14:21:37Z → 2026-08-29T20:32:10Z, zero errored; 3 of them are BPF-loader transactions (deploy `4DucDFEb…` @488892144, extendProgram `4xK82ayR…` @489249015, upgrade `2baq433j…` @489249058 = 2026-08-28T06:50:14Z) and at least one carries two mandate instructions (`3mGrXq5v…`: CreateMandate + Fund), so the instruction histogram counts instructions, not transactions. Deployed source = `programs/mandate/src/lib.rs` at **`56c719f0`** (2026-08-27T19:20:36Z, last change before the deploy); `main` (`c3a3b434`, 2026-08-29) post-dates the deploy. | 2026-09-01 | `solana program show 5o8E…`; `getSignaturesForAddress` full walk; `getBlockTime`; `gh api …/commits?path=programs/mandate/src/lib.rs` |
| PROGRAM_ID_PREDECESSOR | `CT2ntFzcG68wx3XXjjcmZRCKjDE52tZtZYirCe6AadPA` — `MarkovFyi/markov-program` "Phase 0 M1" mandate. Authority `Af1HZgvuD16XVYQzzQTiKH3hToFKx12NPyohpWwBykPy`, slot 487093766, 364336 bytes. Only 4 transactions, all deploys on 2026-08-23; **no receipt ever emitted**. Its 13-name `BlockReason` set (`Unauthorized, MandatePaused, MandateRevoked, MandateExpired, VenueNotAllowlisted, TokenNotAllowlisted, PerTxCapExceeded, UtcDayCapExceeded, X402BudgetExceeded, OperatorNotRegistered, InvalidStateTransition, FailClosed, VenueExecuteNotShipped`) is therefore not bound by the append-only rule. Not the Gate B base. | 2026-09-01 | `solana program show CT2n…`; signature walk; `state.rs`, `SPEC.md`, `docs/FACTS.md` in `MarkovFyi/markov-program`; `packages/idl/src/block-reason.ts` in `MarkovFyi/markov-sdk` |
| PROGRAM_IDL_SHA | **No on-chain IDL account** for either program (Anchor IDL PDA `9GQArvHfqCMCmRPLkssq9iyZSm2CoFzc1cSPabUij1sT` for `5o8E…` and `4uoX4w8C5HirJ9J1Pk5m7NuzJC46NiHNRzwGmfgy2e27` for `CT2n…` both absent). Checked-in IDL `kunaldrall29/markov:packages/operator/idl/mandate.json` at commit **`d26ebad2`** (2026-08-28T06:53:02Z, three minutes after the deploy; "copied from target/idl" per upstream FACTS) — **sha256 `55c62d4b946de7397b2359c6861bb641a10b0877fea4eb4cef9667df8b27196d`**, 32549 bytes, `metadata.version 0.1.0`, `spec 0.1.0`; **snapshot committed here as `docs/idl/mandate-5o8E-55c62d4b.json`**. Consistency with the deployed binary: 8 of the IDL's 9 events were seen on chain and all 8 discriminators match (`PolicyAmended` never emitted); the `BlockReason` discriminants 0–10 decoded from 20 on-chain `ActionRefused` payloads match the IDL enum order; instruction discriminators corroborated for 10 of 12 (`execute_withdraw_venue` and `amend_policy` never invoked on chain). | 2026-09-01 | `dump-idl.mjs` (Kit PDA derivation + `getAccountInfo`); `sha256sum`; `disc.py` (byte 81 of `ActionRefused`); `hist.py`; `gh api …/commits?path=packages/operator/idl/mandate.json` |
| UPGRADE_AUTHORITY | **Successor (decided 2026-09-02, ADR-004): `8wuYJD6bZjSA115mwXgguPoUzSqEP3dc3GxBpCu4M3mn`** (`keys/deployer.json`, generated 2026-09-02T10:50Z; program ID PENDING until P01 generates the program keypair). Predecessor: **`2fpQvTynG9cnCXUqsrJ8CvpJZsNykehMdSv4nkJVStGg`** for `5o8E…`, `demo_swap` and `demo_yield` — Kunal does not hold it ("create new wallet"). Balance 6.7469 SOL; ≥1000 signatures 2026-08-27 → 08-29. Key file is `keys/deployer.json` in the working tree that ran the 27–29 Aug sessions (gitignored; **custody not confirmed by this session — D3 input**). | 2026-09-01 | `solana program show`; `solana balance`; `Anchor.toml [provider] wallet = "keys/deployer.json"` and SESSION_LOG in `kunaldrall29/markov` |
| DEMO_PERPS_ID | PENDING (not built). Existing stub venues of `5o8E…`: `demo_swap` **`3HwcGXdsbfaAov2rYhDtnyeeEbuFVXUaT5GASTCjUUSK`** (slot 489248688, 236832 B) and `demo_yield` **`GsE3vpoBb26vZWbPBbtMACwVem2qgw7whouTLwAAhyzC`** (slot 489248781, 237408 B), same authority. Pools/vaults in `data/devnet.json`: swap pool `7uN8ZGqgy3pfmzz89CPvzBheEmyCFH6BBUGDSxqYQhdV`, yield pool `97nuXo5qWJcudP6yNd3sQAxGCp2TRmvLbHduegjomaze`. | 2026-09-01 | `solana program show`; CPI targets in the 98-tx histogram (11 `Swap`, 2 `Deposit`); `data/devnet.json` |
| REGISTRY_PDA | PENDING — the deployed program has **no registry / global halt**. | 2026-09-01 | `lib.rs` read in full |
| DEMO_MANDATE_PDA | PENDING for Gate B. Existing mandate PDAs (seeds `["mandate", owner, seed_le_u64]`, **not** the pack's `[mandate, owner, strategy_id, nonce]`): `8wEAR5oSYzKrRtLki8H7E87TcHaDYbzuFT7L2cjnPnJo` (four-beat), `4YQ5Xm7qxxC6UjDHPSh8F2Md5dorb8hsKbcWpfrM21X4` (wallet demo, momentum `strategy_id`), `ERUee9ziZwBcpAVtGs8U7KcpgwmvpxDkLMgDG756vFM5` (latest receipt). 20 `MandateCreated` events total. | 2026-09-01 | `lib.rs` seeds; hosted `/v1/receipts`; `kunaldrall29/markov` FACTS |
| VAULT_ATA | PENDING | | |
| ACCOUNT_LAYOUT | `Mandate::SPACE = 8 + 673` (681 bytes on chain). Fields: owner, operator, emergency, quote_mint (4×Pubkey), state u8 (0 Active / 1 Paused / 2 Revoked — **no Expired state**, expiry is a gate), created_ts, expires_ts, day_stamp, spent_today, spend_today, nonce, yield_shares, seed, bump, `Policy`, `strategy_id: Option<[u8;32]>`. The borsh-serialized struct is **519 bytes**, so the allocation carries **154 bytes of unnamed zero slack** (bytes 527..681 are all zero on the three inspected mandate accounts) — there is no declared reserve field, but up to 154 bytes could be appended without a realloc. `Policy` = programs[4]+program_len, tokens[4]+token_len, per_tx_cap, daily_cap, spend_per_call_cap, spend_daily_cap, max_slippage_bps u16 (292 bytes). No `max_mark_age_slots`, no `allowed_actions`, no `expiry` in policy. | 2026-09-01 | `lib.rs` @`56c719f0`; IDL `types`; verification pass 2 (`getAccountInfo` on `8wEAR5…`, `4YQ5Xm…`, `ERUee9…`, tail bytes zero) |
| INSTRUCTIONS_DEPLOYED | `register_operator, create_mandate, fund, amend_policy, pause, unpause, revoke, owner_withdraw, execute_swap, execute_deposit, execute_withdraw_venue, spend` (12). No `init_vault`, `close_mandate`, `set_global_halt`, `execute_venue_action`. Refusals **already emit and return `Ok(())`**; `owner_withdraw` has **no state check**; `unpause` is `OwnerOnly`; emergency = pause/revoke only (`check_emergency_or_owner`). `amend_policy` is **not** tighten-only (any valid policy accepted). Events use `emit!` (`Program data:` logs), not `emit_cpi`. **`main` is not the deployed bytecode:** commit `c3a3b434` (2026-08-29, after the deploy) adds `token::authority = owner` on the `OwnerWithdraw` destination account and refactors `&Account<Mandate>` → `&Mandate`; the deployed binary corresponds to `56c719f0`. | 2026-09-01 | `lib.rs` @`56c719f0` and `gh api …/compare/56c719f0...c3a3b434`; IDL; 98-tx instruction histogram (`ExecuteSwap` 29, `Fund` 20, `CreateMandate` 20, `Spend` 10, `Revoke` 6, `OwnerWithdraw` 4, `RegisterOperator` 3, `ExecuteDeposit` 2, `Unpause` 2, `Pause` 2) |
| GATE_ORDER_DEPLOYED | `Paused → Revoked → Expired → Unauthorized (caller ≠ operator) → ProgramNotAllowed → TokenNotAllowed → OverTxCap → OverDailyCap → SlippageExceeded` (swap path); spend path substitutes `OverSpendCap → OverSpendDailyCap` after the token gate. Differs from `docs/10-PROGRAM-SPEC.md` §3 (which adds GlobalHalt, DuplicateIntent, ActionNotAllowed, StaleOracle, VenueRejected, PostCheckFailed and orders state before halt). | 2026-09-01 | `gate_state`, `gate_swap`, `gate_move` in `lib.rs` @`56c719f0` (unchanged in `c3a3b434`) |

## Mints
| Key | Value | Verified | Source |
|---|---|---|---|
| USDC_D_MINT | **PENDING — Gate B mint, to be created by deployer `8wuYJD6b…` and labelled as such (ADR-004, decided 2026-09-02).** The `5o8E…` world's USDC-d is `6eDVRpGLcBeYfazqsUN9tyeW7NEKjgE3TSgs7yz1YmvC` (SPL Token classic, decimals 6, supply 3,002,574.000000, **mint and freeze authority both `2fpQ…`**, 147 `postTokenBalances` rows across the 98 txs); it stays with that program. The `CT2n…` world's `J83TSeZ1RXEvGpc76bZsZMcFtuPbJHAhUrmyjYc8wdWH` (authority `Af1H…`) is a third mint. | 2026-09-01 / 02 | `getAccountInfo` jsonParsed; `mints.py` over all 98 txs; `data/devnet.json` |
| SOL_D_MINT | PENDING — created together with the Gate B USDC-d by the deployer (ADR-004). No SOL-d exists today; the `5o8E…` world's second mint is **DEMO** `54XpmsacxMvrWPFXrtSjYkconsWm4Xpd7sFPUH1Z4zhd` (decimals 6, supply 10,000,000, authority `2fpQ…`). | 2026-09-01 / 02 | same |
| USDC_D_DECIMALS | 6 | 2026-09-01 | mint account |

## BlockReason enum (decoded from the deployed `5o8E…` program — APPEND ONLY)
Discriminants were read from the `reason` byte of 20 on-chain `ActionRefused` events (offset 81 in the borsh payload), cross-checked against the program log line `ActionRefused <Name>` in the same transaction, the `lib.rs` enum order, and the checked-in IDL. All four agree. **These eleven, in this order, are frozen.** New variants append at 11+.

| # | Name | First emitted (sig) | Slot / time (UTC) | Verified |
|---|---|---|---|---|
| 0 | `OverTxCap` | `3ajx6eZ67oJGGsL5TUzHvhLGrW3wBaXXng7kYxxzu7DGQmL7B3jJ1tBrDZxjGMudS6pex1yF3rD9b2rj4gtkq6cR` | 489250149 · 2026-08-28T06:53:14Z | 2026-09-01 |
| 1 | `OverDailyCap` | `2o4HvZShnyqEVvLrbxcd9kHechjcUaAvPBtEheW9BqU2SYdRJys8Lh9DK1rn87Y3D6NbCCK7HokRQ9pe4uXw1xtb` | 490057931 · 2026-08-29T20:07:53Z | 2026-09-01 |
| 2 | `OverSpendCap` | `3BWrnwFAwZJ4T21UedSqqsNzN9YMa4Lah4hcEXKUseWvkeK6u8zLrh1qBoP6QfRMVqzrBq6j8uWDrkNYEPyzXDEb` | 490057937 · 2026-08-29T20:07:54Z | 2026-09-01 |
| 3 | `OverSpendDailyCap` | `48rxsnSqyKLX5HSL6YhMSaeY8Vxb2w4do2RL5eSCQW7wDGy9zpdsHB8DJFRFAKQFWFyqnoSrvvayP61gqzr8buhB` | 490057959 · 2026-08-29T20:07:58Z | 2026-09-01 |
| 4 | `ProgramNotAllowed` | `5WYsaxf5jDFdkN1LMQ9peF8EVxofmEvcHKMC8xZfEFCQb1ehxtcoXVGu32Q3iEAnYAAxey4AnCstmPoqV9wrKtUa` | 490057893 · 2026-08-29T20:07:47Z | 2026-09-01 |
| 5 | `TokenNotAllowed` | `39t5zf1zCbUYvE5gkUE3YSSF8jhPzWHYPun6tHnv2RwThMXk7PDJSbiLAXXcT3uSxzCT3s98GbBzUX4GtEfpAa7u` | 490057915 · 2026-08-29T20:07:51Z | 2026-09-01 |
| 6 | `SlippageExceeded` | `8ctcEEoD95bS7xnTXtJepaiv3Lr3poryvwZj1CKNrnpPauD8gXKb1MnpV5F9UVDwsoiJYwjKzyvQYCxLrre334P` | 490057962 · 2026-08-29T20:07:58Z | 2026-09-01 |
| 7 | `Expired` | `LCtb5nHDARCNuHDmGM5T4NFGsMnNrLaqBjqd733bvhGpAGh3NG46uwTyNQK3zHCEXXdpdYMeULtibiCSkBmuySc` | 490057877 · 2026-08-29T20:07:44Z | 2026-09-01 |
| 8 | `Paused` | `4DUZvXKMM7TU4VLHFspxF7fwewAcXCkL5tLB7dZZ4qqrNj4Z87D6duEUyfSqYRngcmmHSgFhJ37LoSjsGFW4rDBW` | 490057745 · 2026-08-29T20:07:23Z | 2026-09-01 |
| 9 | `Revoked` | `3vZt8w1yzn799rFrBW8yMNqARnV29K6TzhX7P1FN7puAQJhF7MM2wSBg9vfmSzLTpDTZTiXaAQyqHYTd7br7mwcU` | 489250156 · 2026-08-28T06:53:16Z | 2026-09-01 |
| 10 | `Unauthorized` | `4KRnfoC51QHhfG5e5CqsKNZjMRT45Xf1nLUFEAMuL8i1gFSjFG8BWUfugKrszV7JWbinz4TxYorTcvujbsnMECjG` | 490057880 · 2026-08-29T20:07:45Z | 2026-09-01 |
| 11+ | *(to append in Gate B, names from `docs/10-PROGRAM-SPEC.md` §4: `StaleOracle`, `ActionNotAllowed`, `DuplicateIntent`, `GlobalHalt`, `VenueRejected`, `PostCheckFailed` — order to be fixed in P02, never reordered after first emit)* | — | — | — |

Counts on chain: `OverTxCap` ×6, `Revoked` ×5, every other reason ×1 (20 refusals, 21 `ActionExecuted`). **Spec reconciliation required:** `docs/10-PROGRAM-SPEC.md` §4 numbers `Paused` as 0 and names `NotOperator` / `VenueNotAllowed`; the chain says `OverTxCap` is 0 and the names are `Unauthorized` / `ProgramNotAllowed`. The deployed program wins (ADR-004).

Deployed event set (IDL discriminators, all but one seen on chain): `ActionExecuted` (21), `ActionRefused` (20), `MandateCreated` (20), `MandateFunded` (20), `Revoked` (6), `OwnerWithdrew` (4), `Paused` (2), `Unpaused` (2), `PolicyAmended` (0 — never emitted).

## Keys (public only)
| Key | Value | Verified | Source |
|---|---|---|---|
| OWNER_DEMO_PUBKEY | **`5RPxDN9hxG3YaBSZo6TWfDx1CUGpJKFsuP3hmjUkd1Hv`** (`keys/owner-demo.json`, generated 2026-09-02T10:50Z, 0 SOL). Predecessor demo owner signer: `526oKwSqS7mGTBRbJLFqXwZxygLvGZicd9RGYRrWafTq` (signer on 50 of 98 txs; `keys/owner.json` in the prior workspace; 0.3055 SOL). Signer tallies sum to 100 > 98 because two Revokes were co-signed by owner and emergency. | 2026-09-01 | signer histogram; `solana balance` |
| OPERATOR_PUBKEY | **`EU8a73vNg3Ti4DXtnXF41JLhc78um17er9LDZgsWCbNY`** (`keys/operator.json`, generated 2026-09-02T10:50Z, 0 SOL; goes only into the `book-one` service env). Predecessor house operators: `markov-steady` `AFmFYWsn7hijB54y45Tvs8XQxuy1uG9MRQXDqThKXXBs` (19 sigs), `markov-momentum` `CoVy9jnkzdxzi2say1tpHgiwDbcJ4n7RTvVwp8Rt8bHZ` (10), `markov-redteam` `3ASMCGmhgjuhgVQB672tygz1FYxZk44Fq7rjpCS9jDWB` (15). | 2026-09-01 | `data/house-operators.json`; signer histogram |
| EMERGENCY_PUBKEY | **`A67Gw8VZbYx6qEvFeDdq4eGzpz33L3BkqyhpFrCN7JxB`** (`keys/emergency.json`, generated 2026-09-02T10:50Z, 0 SOL; goes only into the `bot` service env). Predecessor: `6rkSFPh6HWYfT9dMhgiHQ7wV8Fxs7rK1RhgMmKJUFoQz` (6 sigs). **A variable named `EMERGENCY_KEY_JSON` is set on the Railway `api` service and absent on `bot`** (values were never read); `apps/api/src/chain.ts` loads the emergency signer from that variable or `keys/emergency.json`, so the inference is that the API service holds the emergency key and the bot only calls the API. That is the opposite of `docs/14` §2 (emergency key in its own service). | 2026-09-01 | `data/house-operators.json`; Railway `get-service-config` variable *names* for `api` and `bot`; `apps/api/src/chain.ts` |
| MARK_POSTER_PUBKEY | **`2Sx9UFGkkHf1sQ2xmPQyHgNcP7HiVnxoQRg8btzhM5Bv`** (`keys/mark-poster.json`, generated 2026-09-02T10:50Z, 0 SOL; used only by the P04 house poster fallback). | 2026-09-02 | `solana-keygen pubkey` |
| DEPLOYER_PUBKEY | **`8wuYJD6bZjSA115mwXgguPoUzSqEP3dc3GxBpCu4M3mn`** (`keys/deployer.json`; successor upgrade authority and Gate B mint authority; the CLI default signer on this box). **0 SOL — the faucet refused 2 SOL at 10:50Z; Kunal funds this address or the faucet is retried.** | 2026-09-02 | `solana-keygen pubkey`; `solana airdrop`; `solana balance` |
| KEY_CUSTODY | Where `keys/deployer.json`, `keys/owner.json`, `keys/op_dca.json`, `keys/emergency.json` physically live is **unknown to this session** (gitignored and dockerignored; the sessions that used them ran in a `/workspace` checkout, not this box). `~/.config/solana/` on this box was created today by `solana config set` (cli/config.yml + agave-install config) and **holds no keypair** (`id.json` absent). Session 0's only keypair was a scratchpad throwaway for the faucet test. **Gate B keys (2026-09-02):** five keypairs under `/home/kunal/markov.trade/keys/*.json`, mode 600, gitignored (`git check-ignore` verified), custody rules in `keys/README.md`; private keys move into exactly one service env each and never into git or CI. | 2026-09-01 / 02 | `.gitignore`, `.dockerignore`, `apps/api/src/chain.ts` paths; `ls ~/.config/solana ~/.config/solana/id.json` |

## Price source
| Key | Value | Verified | Source |
|---|---|---|---|
| MARK_SOURCE | **`onchain`** — the devnet Pyth `PriceUpdateV2` account `7UVimffx…` read on chain (program, `pyth-solana-receiver-sdk 2.0.0`) and off chain (agent, paper). House poster (`source = house`) built in P04 as the fallback. Decided 2026-09-02 (ADR-003). | 2026-09-02 | ADR-003 |
| HERMES_URL | `https://hermes.pyth.network` — **`/v2/updates/price/latest`, `/v2/updates/price/stream` and v1 `/api/latest_price_feeds` return `401 unauthorized`**; `/v2/price_feeds` (metadata) returns 200. Pyth docs: "Authentication becomes required on August 26, 2026 at 16:00 UTC … Hermes now requires an API Key" (Pyth Pro key, `/price-feeds/pro/acquire-api-key`) or a node provider (Triton, P2P, extrnode, Liquify). Public limit was 10 req / 10 s / IP. `hermes-beta.pyth.network` behaves identically. | 2026-09-01 | `curl` with three user agents; `docs.pyth.network/price-feeds/core/how-pyth-works/hermes` and `…/api-instances-and-providers/hermes` |
| SOL_USD_FEED_ID | `ef0d8b6fda2ceba41da15d4095d1da392a0d2f8ed0c6c7bc0f4cfac8c280b56d` (`Crypto.SOL/USD`) | 2026-09-01 | `GET /v2/price_feeds?query=SOL/USD` on hermes and hermes-beta |
| PYTH_RECEIVER_PROGRAM | `rec5EKMGg6MxZYaMdyBfgwp4d5rB9T1VQH5pJv5LtFJ` — listed for **Solana Devnet** in the Pyth docs table; on devnet executable, **last upgraded slot 491006444 (≈ 2026-08-31)**, authority `upg8KLALUN7ByD…`. The address did not change with the August upgrades. | 2026-09-01 | docs page; `getAccountInfo` on program + ProgramData; `getBlockTime` |
| PYTH_PRICE_FEED_PROGRAM | `pythWSnswVUd12oZpeFP8e9CVaEqJg25g1Vtc2biRsT` — docs table Solana Devnet; on devnet executable, last upgraded slot 293898740 (2024-04-22). It **is** the push oracle (`DEFAULT_PUSH_ORACLE_PROGRAM_ID` in `pyth-crosschain` `pyth_solana_receiver/src/address.ts`). `HDwcJBJXjL9FpJ7UBsYBtaDjsBUhuLCUYoz3zr8SWWaQ` is the **Wormhole core bridge** (`DEFAULT_WORMHOLE_PROGRAM_ID`), upgraded 2026-08-25 — not a Pyth oracle. | 2026-09-01 | docs page; `getAccountInfo`; `pyth-crosschain` `address.ts` via `gh api` |
| PYTH_DEVNET_FEED_ACCOUNT | **`7UVimffxr9ow1uXYxsr4LHAcV58mLzhmwaeKvJ1pjLiE`** — SOL/USD `PriceUpdateV2`, shard 0, PDA of `pythW…` seeds `[u16 0 LE, feed_id]`, **owned by the receiver `rec5…`**, 134 bytes, `verification_level = Full`. At read: price 9999848408 × 10⁻⁸ = 99.998 USD, conf 1501627, `publish_time` 2026-09-01T20:39:19Z, `posted_slot` 491633562, **age 66 slots / 14 s**. Shards 1 and 2 exist but are stale (last posted 2026-03-02 and 2024-07-19); shard 3 absent. **Cadence, 5-minute sample (30 reads 10 s apart, 20:53–20:58Z): 6 distinct `posted_slot`s ≈ one update per ~50 s; `age_slots` min/median/p90/max = 15 / 195 / 317 / 324; `age_s` 5 / 35 / 56.** At today's pacing (`DEVNET_SLOT_MS` ≈ 165 ms) 150 slots ≈ 25 s and 400 slots ≈ 66 s — barely one pusher interval — so a slot-denominated freshness gate sized for ~2.5 minutes needs ≈ 900 slots, or the gate should compare `Clock.unix_timestamp - publish_time` in seconds. The program must also bind the account (owner == `rec5…`, `feed_id` == SOL/USD, `verification_level == Full`) or an operator can pass a stale shard; the stale shards 1–2 are a free `StaleOracle` redteam fixture. Full-day cadence still unmeasured (P08 day 1 logs it). | 2026-09-01 | `pyth-feed.mjs` (Kit PDA derivation, `getAccountInfo` base64, hand-decoded `PriceUpdateV2`), `getSlot`; `pyth-cadence.mjs`; verification pass 2 |
| PYTH_SDK_COMPAT | `pyth-solana-receiver-sdk` **2.0.0** (2026-06-15) requires `anchor-lang ^1.0.2` (+ `pythnet-sdk ^3`); **1.0.1** requires `^0.31.1`; 1.1.0 / 1.2.0 require `^0.32.1`; 0.6.1 `>=0.28`. So a matching SDK exists for either D0 pin. **Build proof done 2026-09-02: 2.0.0 compiles for SBF inside `markov_mandate` on the Anchor 1.1.2 + Agave 4.2.2 pin** (see `SBF_BUILD`). | 2026-09-01 / 02 | `crates.io/api/v1/crates/pyth-solana-receiver-sdk/<v>/dependencies` |
| MARK_MAX_AGE_SLOTS | Superseded: the freshness gate is in **seconds** — `MARK_MAX_AGE_SECS = 150` (≈ three pusher intervals), decided 2026-09-02 (ADR-003); slots are shown for display only. | 2026-09-02 | ADR-003 |
| DEVNET_SLOT_MS | **≈ 165 ms per slot** on devnet 4.3.0-beta.3 (73 slots in 12.1 s at `confirmed`, 2026-09-01 ≈ 21:40Z; verification agents measured 163–172 ms over 8–20 s windows and 166 ms over the 397,206 s since the `5o8E…` deploy). `docs/07` §8's "150 slots (~60 s)" is ≈ 25 s at this pacing; `LAG_SLOTS` must be set from this row, as pack v0.2 requires. | 2026-09-01 | `getSlot` ×2 with wall clock; `getBlockTime` |

## Hosts
| Key | Value | Verified | Source |
|---|---|---|---|
| LANDING_URL | `https://markov.trade` — domain is **registered and delegated to Cloudflare** (`tori.ns.cloudflare.com`, `simon.ns.cloudflare.com`) but has **no A/AAAA/CNAME records**; HTTPS unreachable. Same for `www.` and `api.markov.trade`. | 2026-09-01 | `dig NS/SOA/A`; `curl` |
| BOOK_URL | PENDING host. Decided 2026-09-02 (ADR-002): one origin, `apps/web` on Vercel at `https://markov.trade/book`; `api.markov.trade` → Railway `data-api`. Stays PENDING until it answers logged-out over HTTPS. | 2026-09-02 | ADR-002 |
| RECEIPTS_API_URL | Intended `https://api.markov.trade/v1/receipts` (no DNS). **Existing:** `https://data-api-production-5ac5.up.railway.app/v1/receipts` → 200; `/v1/receipts/stats` → `{total:41, allowed:21, blocked:20}` with all 11 `by_reason` keys. CORS: responds 200 to `Origin: https://markov.trade` with no `Access-Control-Allow-Origin` header; the allowlist is `packages/rpc/src/domains.ts` `isProductOrigin` (used by `apps/data-api/src/cors.ts`): product origins (`*.markovhq.com`), any `https://*.vercel.app`, localhost/127.0.0.1, plus `CORS_ORIGINS` / `WEB_ORIGIN` / `SITE_ORIGIN` extras. `/v1/book/stats`, `/v1/mandates/:a`, `/v1/paper`, `/v1/facts` → **404** on this service; `/price/:symbol` → 402. | 2026-09-01 | `curl`; `cors.ts`, `domains.ts` via `gh api` |
| HEALTH_URL | Existing `data-api /health` → `chainReady:false, rpcOk:false, lastIndexedSlot 490387517, lagSlots 1,245,076` at 20:3xZ (1,265,936 an hour later); slot 490387517 was produced **2026-08-30T11:17:38Z**, so the indexer has been stalled since then. `indexer /health` reports the same slot (`rpcOk:true`). `api /health` → `chainReady:true` (RPC probe only). `apps/data-api/src/health.ts` computes `chainReady = indexer.chainReady === true \|\| lagSlots <= 128` — an upstream boolean short-circuit, the pattern `docs/07` §8 forbids. | 2026-09-01 | `curl`; `getBlockTime 490387517`; `health.ts` |
| RPC_PRIMARY | PENDING — no RPC-provider account or key found on this box, and no provider-shaped variable name on the Railway services (the indexer has `SOLANA_RPC_URL`/`SOLANA_WS_URL`, values not readable; upstream FACTS says they point at public devnet and that the WebSocket 429s). | 2026-09-01 | `env`; Railway variable names; upstream `kunaldrall29/markov` FACTS |
| RPC_FALLBACK | `https://api.devnet.solana.com` (`getVersion` → solana-core 4.3.0-beta.3). Rate-limited this session on `getTransaction` bursts (HTTP 429). | 2026-09-01 | `curl` |
| EXISTING_RAILWAY | Project **`markov`** `9c4577ff-23a5-4ebf-a167-2aa0a10caad7`, workspace "kunal drall's Projects" (user `kunaldrall29`), environment `production`. Services (Dockerfile builds at `apps/{api,data-api,indexer,agents,bot}/Dockerfile`, paths that exist in `kunaldrall29/markov`; the MCP does not expose a git source and upstream SESSION_LOG says they were CLI uploads; all online): `api` `api-production-d2e8.up.railway.app` · `data-api` `data-api-production-5ac5…` · `indexer` `indexer-production-00ef…` · `agents` `agents-production-2720…` · `bot` `bot-production-e30d…` · `Postgres` (volume). Six failed deploys 28 Aug, last successful 29 Aug 20:30 UTC. No custom domains. | 2026-09-01 | Railway MCP `whoami`, `list-projects`, `list-services`, `environment-status`, `get-service-config`, `list-domains` |
| EXISTING_VERCEL | MCP-visible team `kunals-projects-35d3a237` (hobby): `markov-float` (0 deployments; git-linked to **`kunal-drall/markov`, which is the old Base product — stale link**), `float-web` `prj_tcpy7u9riTqUOAZEfoSnf5olJz9B`, `markov-docs`, `markov` (`markov-rosy.vercel.app`), plus `markov-landing-…`, `markov-litepaper-…`, `markov-site-…`. The **live** Float/docs deploys (`float-web-three.vercel.app`, `float.markovhq.com`, `markov-docs-black.vercel.app`) answer 200; upstream `kunaldrall29/markov` FACTS attributes them to Vercel team `lemmalabs` (`lemmalabs1`), which this MCP connection cannot see (a *project* named `lemmalabs` exists in the visible team). `kunal-drall/markov` is confirmed to be the old Base product (repo description, pushed 2026-07-22). | 2026-09-01 | Vercel MCP `list_teams`, `list_projects`, `get_project`; `curl`; upstream FACTS; `gh api repos/kunal-drall/markov` |
| EXISTING_SITES | `https://markovhq.com` 200 (Next.js marketing site, `kunaldrall29/markov-landing`); `/litepaper` is **v0.7** (August 2026; "previous version v0.4"; v0.6.1 appears in its changelog) and names `5o8E…` in its live-artifacts section. `https://markovhq.grok.me` 200 — the **Markov Book design**: TanStack Start (`$_TSR`, `/assets/{index,routes,book,book-store,blotter,footer}-*.js`), Tailwind v4; `/book` exists and is a **static sample blotter** (no fetches, no program IDs, header `BOOK_ONE · demo_perps`, copy `Demo blotter — not mainnet capital`). | 2026-09-01 | `curl`; asset grep |
| RAILWAY_MARKOV_DEVNET | **New Railway project `markov-devnet`** `7c023a8a-60be-4f2a-bc71-948bc3b2fdeb` (workspace "kunal drall's Projects"), environment `production` `af0f3483-5726-4c53-b563-3050e387d9ef`. Service **`book-one-paper`** `a625dcc8-7db7-452e-a966-52ede6255668`: Dockerfile builder (`crates/book-one/Dockerfile`), restart ALWAYS, volume `paper` `ed6bd06e-2ddb-4edd-af81-166ee19e01d6` mounted at `/data/paper`, no public domain (the runner exposes no HTTP). Variables (names): `VENUE=shadow`, `TICK_SECONDS=60`, `MARK_SOURCE=onchain`, `PAPER_DIR=/data/paper`, `PAPER_START_DATE=2026-09-02`, `RUST_LOG`, `RPC_HTTP_URL`/`RPC_HTTP_FALLBACK` (public devnet until Kunal's key), `PYTH_PRICE_UPDATE_ACCOUNT`, `SOL_USD_FEED_ID`, `MARK_MAX_AGE_SECS=150`, `PER_TX_CAP=50`, `RAILWAY_DOCKERFILE_PATH`, `RAILWAY_RUN_UID=0`. Deployed with `railway up` from this directory (no GitHub remote); deployment `87023807-8abe-4875-9ab4-e44575ca7089` SUCCESS, release build 1 m 39 s, image 37 MB snapshot. **Hosted first tick 2026-09-02T11:42:31Z** (`20260902T114231Z-000001`, slot 491960279, mark 12 s old, skip).  Redeployed with the review-fixed build (deployment `ceaec520…`, SUCCESS 12:08Z): it resumed the volume's tick log ("resuming today's log; holding the cadence 41 s", next tick `…-000027`). | 2026-09-02 | Railway MCP `create-project`/`create-service`/`create-volume`/`set-variables`; `railway up`; `railway logs -d` |
| RAILWAY_LESSONS | (1) A `.railwayignore` listing `apps/ packages/ landing/ docs/ paper/ node_modules/ target/ keys/ .github/ *.md` made two uploads fail at `BUILD_IMAGE` within 10 s with no build log; the same tree without it built — cause not bisected (BACKLOG). (2) A Dockerfile `VOLUME` instruction is rejected ("docker VOLUME … is not supported, use Railway Volumes"). (3) Railway volumes mount root-owned; a non-root `USER` in the image gets `Permission denied`; `RAILWAY_RUN_UID=10001` did not chown, `RAILWAY_RUN_UID=0` (run as root) did. (4) `railway.toml` config-as-code is deprecated (`update-service` refuses it). | 2026-09-02 | deployments `dcd9ecc2…`, `5ea3b174…`, `26172086…`, `87023807…` and their logs |
| PAPER_RUNNERS | Two shadow runners write day files today: the **local** one on this box (pid in the session scratchpad, writes the committed `paper/YYYY-MM-DD.md`) and the **hosted** one on Railway (writes `/data/paper` on the volume). The committed file is the local runner's; the hosted copy is the resilience against this box dying. Until P10's `/v1/paper` serves the volume, hosted files are read with `railway ssh`/`railway run` by hand. Two files for one day differ in tick counts and start time and are both honest. | 2026-09-02 | this session |
| EXISTING_BOT | Telegram `@markov_float_bot` (`tokenSet:true` on the Railway `bot` service; token in Railway env only). | 2026-09-01 | `bot /health` |

## Web app and pack provenance
| Key | Value | Verified | Source |
|---|---|---|---|
| ENGINEERING_PACK | **v0.2** (31 Aug 2026), adopted into `docs/`, `prompts/`, `landing/`, `scripts/`, `README.md`, `CORRECTIONS.md` at commit `13c431d`; v0.1 (the pasted pack) is in history. Source: `kunaldrall29/markov-landing-new@e4e6733` `artifacts/engineering-pack/`. Planning pack 00–06, `GATE-B.md` and the Grok brief are under `docs/planning/`. | 2026-09-01 | `diff -rq`; `git log` |
| WEB_APP_SOURCE | `github.com/kunaldrall29/markov-landing-new` (private, 2 commits "Export from Grok", latest `e4e6733` 2026-09-01T21:07Z) imported verbatim to **`apps/web/`** at commit `f505908`; exclusions listed in `apps/web/IMPORT.md`. Routes: `/`, `/book`, `/receipts`, `/paper` (all static sample data from `src/lib/tape.ts`; no fetches, no program IDs, no API hosts). | 2026-09-01 | `gh api`; clone; `grep` |
| WEB_APP_STACK | `@tanstack/react-start ^1.168.0`, `@tanstack/react-router ^1.170.0`, `vite ^8.2.0`, `tailwindcss ^4.3.0` + `@tailwindcss/vite`, `react ^19.2.0`, `nitro 3.0.260610-beta` (preset `vercel`), `lucide-react ^0.510.0` (npm latest is 1.39.0), `zustand ^5`, `typescript ^5.7`, plus Grok scaffolding deps (`better-auth`, `@electric-sql/pglite`, `kysely`, `pg`, Radix). `.grok/app-env.json` sets `VITE_AUTH_ENABLED=false`; the Vercel deployer sets it `true`. No `@solana/*` dependency yet. Tokens in `src/styles.css` match `docs/13` §1 byte for byte. | 2026-09-01 | `package.json`; `src/styles.css` vs `docs/13` |
| WEB_APP_BUILD | `npm ci && npm run build` succeeds on node 24.19.0 (nitro → `.vercel/output/`, `db:migrate` skips without `DATABASE_URL`). `npm run preview` serves it; `/`, `/book`, `/receipts`, `/paper` → HTTP 200 (31653 / 12205 / 10203 / 9271 bytes). | 2026-09-01 | scratchpad build + `curl` |
| COPY_GREP_APP | Rendered HTML of all four routes: **clean** under a correct strip (638 / 131 / 101 / 170 visible words), and the injected `Earn 12% APY, guaranteed. Built on Jupiter.` test goes red. **But `scripts/copy-grep.sh` as shipped is vacuous on this output:** the SSR HTML is one line and its `sed 's|<script[^>]*>.*</script>||g'` is greedy, deleting everything between the first `<script>` (theme boot in `<head>`) and the last `</script>` (module loader before `</body>`) — 1 word survives per page, so "clean" proves nothing. Fix is a non-greedy multiline strip (`perl -0pe 's/<script\b[^>]*>.*?<\/script>//gs'`), see `docs/BACKLOG.md`. | 2026-09-01 | word counts before/after strip; both runs |
| BOOK_LABEL | `/book` copy reads "Sample tape, devnet. Marked PnL, not a promised rate. Unaudited." — the B1 literal `devnet · marked PnL, not a promised rate` is not present verbatim above the fold; the header shows `DEVNET`; the blotter says `marked · not a rate`. P11 item. | 2026-09-01 | `src/routes/book.tsx` |

## Workspace (P01, 2026-09-02)
| Key | Value | Verified | Source |
|---|---|---|---|
| SUCCESSOR_PROGRAM_ID | **`25CdYaZeB18QvUR7cTyZPgTZPNREb7t6xL8zmk1eXAU6`** — program keypair generated 2026-09-02 (`keys/markov-mandate-keypair.json`, gitignored; copied to `target/deploy/markov_mandate-keypair.json` for Anchor). **Not deployed.** Becomes `PROGRAM_ID` only after `solana program show` returns it. | 2026-09-02 | `solana-keygen pubkey`; `declare_id!` in `programs/markov-mandate/src/lib.rs` |
| DEMO_PERPS_ID | **`3Zcd8XsFWBTVku5GxQjwEBC7sLrJhF8vadyTnTr56hxB`** — keypair generated 2026-09-02 (`keys/demo-perps-keypair.json`). **Not deployed.** | 2026-09-02 | same |
| ANCHOR_TOML_PINS | `[toolchain] anchor_version = "1.1.2"`, `solana_version = "4.2.2"`; `[provider] cluster = "devnet"`, `wallet = "keys/deployer.json"`; `[scripts] test = "cargo test -p markov_mandate -p demo_perps"` (LiteSVM, not Surfpool). | 2026-09-02 | `Anchor.toml` |
| RUST_TOOLCHAIN_TOML | `channel = "1.97.1"` (host services and tests); workspace `rust-version = "1.89.0"` (the Anchor 1.1.2 template minimum). | 2026-09-02 | `rust-toolchain.toml`, `Cargo.toml` |
| PLATFORM_TOOLS | `cargo-build-sbf 4.1.0` (Agave 4.2.2) defaults to platform-tools **v1.54**; anchor-cli 1.1.2 passes `--tools-version v1.52` (string in the binary) and linked rustup toolchain `1.89.0-sbpf-solana-v1.52`. **Hazard found:** a 520 MB v1.54 extraction interrupted by a killed foreground call left `rustc` without `librustc_driver`, after which every `cargo-build-sbf` invocation (even `--version`) failed and `anchor build` panicked at `toolchain.rs:357` (`Option::unwrap` on an empty `rustc --version`). Fix: `rm -rf ~/.cache/solana/v1.54` and never run the SBF build inside a call that can be killed. | 2026-09-02 | `anchor build` logs #1–#3; `cargo-build-sbf-4.1.0/src/toolchain.rs` from crates.io; `strings ~/.avm/bin/anchor-1.1.2` |
| WEB_WORKSPACE | pnpm workspace (`apps/*`, `packages/*`), `turbo 2.10.12`; `pnpm install` resolves `apps/web` to `@tanstack/react-start 1.168.49`, `@tanstack/react-router 1.170.32`, `vite 8.2.2`, `tailwindcss 4.3.3`, `react 19.2.8`, `nitro 3.0.260610-beta`, `lucide-react 0.510.0`, `typescript 5.9.3` (`pnpm-lock.yaml`, committed). `pnpm run build` and `pnpm run typecheck` (turbo) **green**: `apps/web` builds via nitro `vercel` preset, `tsc --noEmit` clean; `@markov/sdk` is a stub. | 2026-09-02 | turbo output; `pnpm-lock.yaml` |
| COPY_GREP_FIX | `scripts/copy-grep.sh` strip is a non-greedy multiline `perl` strip; **attribute text is extracted first** (`content`, `alt`, `aria-label`, `title` → moved outside the tag as `>value<`, review finding), and the `too-few-words` rule fires only when the stripped page keeps < 40 words **and** < 10 % of the tags-only word count (terse pages pass). Proven: static landing clean; four rendered `apps/web` routes clean (638 / 131 / 101 / 170 words); injected body claim → 4 FAILs; claim only in a meta description → 4 FAILs; only in `alt` → `risk-free`, `audited-claim`; only in `aria-label` → 2 FAILs; only in a link `title` → `eleven-refusals`; an honest meta description and a 12-word page pass. | 2026-09-02 | script self-tests |
| TRUTH_SCRIPTS | `scripts/no-keys-in-tree.sh` (64-byte JSON arrays, secret-key fields, PEM blocks; a base58 length rule was dropped because a secret key and a signature are both 64 bytes) — clean on 170 tracked files, catches a staged keypair-shaped file. `scripts/guard-purity.sh` (`cargo tree` on markov-guard, bans I/O, clock and RPC crates) — clean. `scripts/render-routes.sh` renders TanStack routes for copy-grep in CI. | 2026-09-02 | self-tests |
| CARGO_DENY | **Corrected 2026-09-02 (review):** the first version of this row said `bans ok` for the post-P08 tree without re-running cargo-deny after the four P08 crates landed; on that tree `cargo deny check` was `advisories ok, bans FAILED (wildcard path deps), licenses FAILED (CDLA-Permissive-2.0, webpki-roots via reqwest), sources ok`. Fixed by `allow-wildcard-paths = true` in both deny files and allowing `CDLA-Permissive-2.0`; `unmaintained = "workspace"` scopes unmaintained-crate errors to direct deps (bincode 1.3.3, ansi_term, derivative, libsecp256k1, paste are transitive under anchor/solana/pyth/litesvm). **Re-run 2026-09-02 12:0xZ (acceptance #5): `cargo deny check` → advisories ok, bans ok, licenses ok, sources ok; guard bans ok.** | 2026-09-02 | acceptance log #5; `cargo deny check` |
| CI_WORKFLOWS | `.github/workflows/{rust,program,web,truth}.yml` — a **subset** of `docs/15` §3: rust (fmt, clippy `-D warnings`, build, test on the host crates; the two Anchor programs are excluded because their LiteSVM tests `include_bytes!` the built `.so`), program (Agave 4.2.2 + Anchor 1.1.2 install, `anchor build --ignore-keys`, LiteSVM suite; IDL append-only check is a P14 stub), web (pnpm build + typecheck; **no eslint yet**), truth (render + copy-grep + red-test + key scan; **no parity-check, Playwright, Lighthouse or link-check yet** — P14). **None has run: this repo has no GitHub remote.** `scripts/render-routes.sh` is unproven in CI. Equivalent commands run locally, results in this table. | 2026-09-02 | files; local runs |
| P01_P08_REVIEW | A 3-lens + skeptic review of commits `e25430b`/`c0c6ce6` returned 32 standing findings (1 blocker: the cargo-deny row; 13 should-fix; 18 nits). **Fixed in `review fixes` commit:** deny policy + honest re-run; `rust.yml` excludes the SBF programs and `program.yml` owns `anchor build --ignore-keys` + LiteSVM; paper renderer no longer hard-codes PnL lines (`—`), refuses to render a day that has intents until P06/P07 supply PnL, derives `started late` from the first tick, notes restarts / runner gaps / feed stalls / unparseable rows, appends each tick with one write, never writes `no run` markers onto an unseeded volume, renders days with ticks but no file; `PriceUpdateV2` discriminator checked; saturating mark age; fallback RPC keeps binding refusals; `now` taken after the fetch; interval-paced loop with cadence held across restarts and tick counter seeded from the log; `PAPER_START_DATE` mandatory in shadow mode; `.env.example` blank with defaults in comments; `.dockerignore` excludes env/secrets; key scan catches multi-line arrays and `*_KEYPAIR=` assignments; `docs/09` §4 and `docs/11` §8 amended to seconds (ADR-003); `regime.rs` → `sidecar.rs`. **Deferred with owner:** `/v1/paper` route and `/paper` page (P10/P11); eslint, Playwright, Lighthouse, parity-check, link-check in CI (P14); runbook/demo/script placeholders vs `docs/09` §1 (P14); true random jitter (P07). | 2026-09-02 | workflow `wf_a1bf8611-3a5` |
| SBF_BUILD | **`anchor build` exit 0** (9 m 04 s wall, of which the release SBF compile was 5 m 33 s) on Anchor 1.1.2 + Agave 4.2.2 with platform-tools **v1.52** (anchor's `--tools-version`), rustup toolchain `1.89.0-sbpf-solana-v1.52`. Artefacts: `target/deploy/markov_mandate.so` 57512 B sha256 `36523123dc4ae7eaf5cfc84a5b6f9a2d21c1aa77e6f460b79817ef5aef6bfb18`; `demo_perps.so` 57504 B sha256 `0fbb4a0ef9a8d14d0b56418f3132edcdb0290bd7044088055c15bc58c65c4864`; IDLs `target/idl/markov_mandate.json` sha256 `2e3fd79764e5e20bc0934a2963b3d5a022d34f2a9df12cdd4684b4e1febc8818` (address `25CdYaZe…`, instruction `ping`, spec 0.1.0), `demo_perps.json` sha256 `5738e62e5050bc13ce91929055b991ad30619ab51a9f284395587d1441aad007`. **`pyth-solana-receiver-sdk 2.0.0` is a dependency of `markov_mandate` and compiled for SBF in this build** — P04 pre-flight #1 is answered for the chosen pin. Not deployed (no SOL). | 2026-09-02 | `anchor build` log #3; `sha256sum` |
| HOST_ACCEPTANCE | **After the review fixes (acceptance #5 + #6, 2026-09-02 ~12:05Z):** `cargo fmt --all --check` clean; `cargo build --workspace` ok; `cargo test --workspace` all green — LiteSVM `ping_lands_on_litesvm` on both programs (litesvm 0.16.0), `markov-types` 2, `markov-guard` 5, `markov-marks` 8 (incl. `wrong_discriminator_is_refused_before_any_field_is_read`, `age_never_overflows`), `book-one` 9 (`paper::one_file_per_day`, `no_backfill`, `schema_has_no_apy_field`, `missing_days_get_a_no_run_marker…`, `unseeded_directory_gets_no_markers`, `a_day_with_ticks_but_no_file_is_rendered_not_marked`, `truncated_line_is_counted_not_fatal`, `placeholder_pnl_refuses_to_render_a_day_with_intents`, `started_late_restarts_gaps_and_stalls_are_noted_from_the_log`); `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo deny check` all four ok; `cargo audit` ok (6 allowed unmaintained/unsound warnings, all transitive); guard purity clean; key scan clean on 232 tracked files. The earlier version of this row (before the review) had been written without re-running cargo-deny; see `CARGO_DENY`. | 2026-09-02 | acceptance logs #5, #6 |

## Gate B proofs
| Key | Value | Verified | Source |
|---|---|---|---|
| SIG-FUND | PENDING | | |
| SIG-ACT | PENDING | | |
| SIG-AMEND | PENDING | | |
| SIG-CAP | PENDING | | |
| SIG-REV | PENDING | | |
| SIG-REV2 | PENDING | | |
| SIG-WD | PENDING | | |
| SIG-SLIP-OR-SPEND | PENDING | | |
| PAPER_START_DATE | **2026-09-02** — first file `paper/2026-09-02.md`, first tick 11:26:40Z (`tick_id 20260902T112640Z-000001`, slot 491954551, mark 97.38 USD 21 s old, verdict skip). Started late, noted in the file. **Never edit.** | 2026-09-02 | `paper/2026-09-02.md`; `paper/ticks/2026-09-02.jsonl` |
| GATE_B_STATUS | **OPEN** | 2026-09-01 | |
| GATE_B_CLOSED_DATE | — | | |

Prior-art signatures on `5o8E…` that are *not* Gate B proofs (different program spec, `strategy_id` momentum/redteam, no `BOOK_ONE`): four-beat 2026-08-28 (fund `2iqbzg6s…`, OverTxCap `3ajx6eZ6…`, Revoked `3vZt8w1y…`, withdraw `342HP72T…`); wallet demo `mdt_0038` (create `3mGrXq5v…`, pause `623dpkC8…`, withdraw `Nt7YSyWm…`, revoke `5uBLfQ6T…`). Recorded so nobody re-labels them as B2–B8.

## Venue checklist (ADR-03) — no venue may be named in public copy until all five pass
| Venue | 1 programmatic open/close/cancel | 2 settlement mint | 3 position+funding readable | 4 paper/devnet parity | 5 ToS allows vault agent | Verdict |
|---|---|---|---|---|---|---|
| *(none evaluated)* | PENDING | PENDING | PENDING | PENDING | PENDING | NOT NAMED |

`scripts/copy-grep.sh` run on `landing/index.html`: **clean** (exit 0). Injecting `Earn 12% APY, guaranteed. Built on Jupiter.` produced four FAIL rules (promised-rate, apy-claim, guaranteed, named-venue) and exit 1. 2026-09-01.

## Verification log (append-only; never edit an old entry)

```
2026-09-01  S0  PACK_LAYOUT          pack arrived flat under files/; moved with git mv to docs/ prompts/ landing/ scripts/ (commit 73b0dc3); contents byte-identical to ~/projects/markov.trade/docs (diff -rq)
2026-09-01  S0  RUST_VERSION=1.97.1  cmd: `rustc --version`
2026-09-01  S0  ANCHOR/SOLANA        `anchor`, `solana`, `avm` NOT INSTALLED at session start -> installed Agave stable (solana-cli 4.2.2) via release.anza.xyz, avm 1.1.2 via `cargo install --git coral-xyz/anchor avm`, `avm install 0.31.1`. anchor-cli 0.31.1 prints. Pin still D0.
2026-09-01  S0  FAUCET               `solana airdrop 1` / `0.5` -> "rate limit reached" twice. Balance 0. P01 pre-flight #2 unsatisfied.
2026-09-01  S0  DEVNET_RPC           getVersion -> solana-core 4.3.0-beta.3; getTransaction bursts hit HTTP 429 (needs a provider)
2026-09-01  S0  PROGRAM_ID           two candidates found in kunaldrall29/markov-landing docs/FACTS.md; both executable on devnet. Resolved: 5o8E... (kunaldrall29/markov, 98 txs, 11 reasons emitted) is the base; CT2n... (MarkovFyi/markov-program, 4 deploy txs, never emitted) is the predecessor.
2026-09-01  S0  IDL                  no on-chain IDL for either program (PDA absent). Checked-in IDL sha256 55c62d4b... verified against chain via event discriminators (8/8) and ActionRefused reason bytes (11/11).
2026-09-01  S0  BLOCKREASON          0..10 = OverTxCap OverDailyCap OverSpendCap OverSpendDailyCap ProgramNotAllowed TokenNotAllowed SlippageExceeded Expired Paused Revoked Unauthorized. Contradicts docs/10 §4 numbering and two names. Deployed program wins -> ADR-004 amendment needed before P02.
2026-09-01  S0  UPGRADE_AUTHORITY    2fpQ... holds 5o8E, demo_swap, demo_yield. Key custody not confirmed. -> D3 input, ADR-004.
2026-09-01  S0  HERMES               price endpoints 401 since 2026-08-26 (Pyth docs). Metadata endpoint 200. -> D2 changes shape: the *off-chain* path needs a key or provider too. ADR-003.
2026-09-01  S0  PYTH_DEVNET          receiver rec5... upgraded 2026-08-31 (slot 491006444), address unchanged; price-feed program pythW... unchanged since 2024.
2026-09-01  S0  PYTH_SDK             2.0.0 -> anchor ^1.0.2; 1.0.1 -> anchor ^0.31.1. Pack's "compatible up to 0.31.1" is stale. Build proof still pending (P04).
2026-09-01  S0  VERSIONS             pack says Anchor 1.0.0/1.0.1; crates.io says 1.1.2 stable, 2.0.0-rc.1. Recorded, not pinned.
2026-09-01  S0  HOSTING              existing Railway project `markov` (6 services, online) and Vercel deploys for the 5o8E stack found; indexer stalled (lag 1.2M slots, chainReady false). Not in the pack. -> D4, ADR-005.
2026-09-01  S0  EMERGENCY_KEY        EMERGENCY_KEY_JSON is a variable of the Railway `api` service, not `bot`. Violates docs/14 §2 separation for the existing stack. Gate B services must not inherit this.
2026-09-01  S0  DOMAIN               markov.trade delegated to Cloudflare, no records. D1 must be decided before any DNS is set.
2026-09-01  S0  COPY_GREP            landing/index.html clean; injected APY+venue -> 4 FAILs, exit 1. B15 tooling works as shipped.
2026-09-01  S0  PYTH_DEVNET_FEED     SOL/USD PriceUpdateV2 shard 0 = 7UVimffx... live on devnet, owner rec5..., Full, age 66 slots / 14 s at 20:39:33Z. Shards 1-2 stale, 3 absent. -> ADR-003 option A is viable; P08 can start with MARK_SOURCE=onchain. Day-long cadence unmeasured.
2026-09-01  S0  PYTH_CADENCE         30 reads over 290 s: 6 updates (~1/50 s); age_slots median 195, p90 317, max 324. -> max_mark_age_slots must be ~400, not 150. Recorded on the PYTH_DEVNET_FEED_ACCOUNT row and in ADR-003.
2026-09-01  S0  ANCHOR_FLIPS_SOLANA  `avm install 1.1.2` auto-switched to 1.1.2 and anchor-cli 1.1.2 ran agave-install init 3.1.10; after `avm use 0.31.1` + `agave-install init 4.2.2`, a bare `anchor --version` (0.31.1) re-initialised 2.1.0. Restored 4.2.2 with agave-install; anchor not invoked outside a project since. -> P01: pin [toolchain] solana_version; ADR-001 consequence.
2026-09-01  S0  LANDING_IMPORT       Kunal pointed at kunaldrall29/markov-landing-new. It is the whole TanStack Start app (/, /book, /receipts, /paper) plus engineering pack v0.2 + planning pack under artifacts/. Imported: app -> apps/web (f505908), pack v0.2 -> docs/prompts/landing (13c431d), planning -> docs/planning (8e4cd5b). No secrets found in the clone (grep for key shapes; .grok/app-env.json holds only VITE_AUTH_ENABLED=false).
2026-09-01  S0  PACK_V0.2_VS_CHAIN   v0.2 CORRECTIONS still lists the "historical eleven" as Paused, Revoked, Expired, NotOperator, VenueNotAllowed, ... ; chain says OverTxCap=0 ... Unauthorized=10 with ProgramNotAllowed/Unauthorized (ADR-004 stands). v0.2 "recommended Agave 3.1.10" is anchor-cli 1.1.2's default, not stable (4.2.2) nor devnet (4.3.0-beta.3). v0.2 "SDK compat stops at 0.31.1" is stale: pyth-solana-receiver-sdk 2.0.0 requires anchor-lang ^1.0.2 (crates.io). v0.2 "@solana/kit 8.1.0 observed" -> 8.2.0 today. v0.2's Hermes-key and 26-Aug-cutover corrections match this session's evidence.
2026-09-01  S0  COPY_GREP_SSR        rendered app HTML is single-line; shipped copy-grep strip is greedy -> 1 word survives -> vacuous pass. Non-greedy strip: 4 routes clean, injected claim red. -> BACKLOG (P14): fix strip_html.
2026-09-02  S1  DECISIONS            Kunal: "create new wallet, i will provide RPC provider key later, for others decide on me". Accepted per recommendations: ADR-001 B (Anchor 1.1.2 + Agave 4.2.2), ADR-002 A (one origin), ADR-003 A with C fallback (on-chain Pyth account, seconds-based freshness 150 s, program-side binding), ADR-004 A (successor program, new mints, enum 0-10 verbatim + 11-16, emit_cpi, on-chain IDL, verifiable build), ADR-005 A (Rust, parity first, new Railway project markov-devnet), ADR-006 accepted.
2026-09-02  S1  KEYS                 five keypairs generated 10:50Z under keys/ (gitignored, 600): deployer 8wuYJD6b..., owner-demo 5RPxDN9h..., operator EU8a73vN..., emergency A67Gw8VZ..., mark-poster 2Sx9UFGk.... CLI default signer = deployer. Faucet refused 2 SOL + 4x0.5 SOL (rate limit) -> all balances 0. Funding is on Kunal or a later faucet retry; P01 proceeds on the parts that need no SOL, deploys wait.
2026-09-02  S1  P01_SCAFFOLD         workspace written (programs/markov-mandate, programs/demo-perps with a scaffold `ping`, nine empty crates, Anchor.toml pins, rust-toolchain.toml, pnpm workspace, CI yml, deny.toml, .env.example, truth scripts). Program keypairs generated (25CdYaZe... mandate, 3Zcd8Xs... demo_perps), not deployed. pnpm build+typecheck green. copy-grep strip fixed and proven. cargo deny: bans ok; advisories scoped to direct deps (bincode/ansi_term unmaintained transitives). cargo fmt clean.
2026-09-02  S1  P01_ANCHOR_BUILD     anchor build exit 0 in 9m04s (platform-tools v1.52 via anchor-cli). markov_mandate.so + demo_perps.so + IDLs produced; pyth-solana-receiver-sdk 2.0.0 compiled for SBF. Host: fmt/build/test/clippy/deny/audit/guard-purity/no-keys all green; LiteSVM 0.16.0 ping tests pass on both programs.
2026-09-02  S1  P08_FIRST_TICK       book-one VENUE=shadow first tick 11:26:40Z: Pyth devnet account read, bound (owner/feed/Full), mark 97.38 age 21 s, verdict skip; paper/2026-09-02.md rendered from the tick log. PAPER_START_DATE=2026-09-02 fixed. Runner left running detached on this box (tick 60 s) until the Railway service takes over.
2026-09-02  S1  P01_P08_REVIEW       3 reviewers + skeptic: 32 findings. Blocker confirmed: CARGO_DENY/HOST_ACCEPTANCE rows had been written without re-running cargo-deny after the P08 crates (bans + licences were failing). Fixed, re-run, rows rewritten. All should-fixes applied (see P01_P08_REVIEW row); nits fixed or deferred to P07/P10/P11/P14. Acceptance #5/#6 all green. Local runner restarted on the fixed binary (resumed at tick 40, cadence held); hosted redeploy ceaec520 uploaded.
2026-09-02  S1  P08_HOSTED           Railway project markov-devnet created; book-one-paper deployed via railway up after three failed attempts (.railwayignore -> instant BUILD_IMAGE failure; Dockerfile VOLUME rejected; volume root-owned -> RAILWAY_RUN_UID=0). Hosted first tick 11:42:31Z. Local runner still running; committed paper file comes from it.
2026-09-02  S1  PLATFORM_TOOLS       anchor build #1 stopped at keypair mismatch (Anchor generates target/deploy keypairs); #2 and a direct cargo-build-sbf failed on a half-extracted platform-tools v1.54 (my 3-min foreground timeout killed the extraction). Removed ~/.cache/solana/v1.54; build #3 relaunched detached.
2026-09-01  S0  VERIFY_PASS_2        13-agent workflow (6 re-verifiers, 3 refuters, 3 judges, 1 critic; 256 tool calls). 7 rows refuted and corrected: SOLANA_CLI_VERSION (active release was 2.1.0 again), ACCOUNT_LAYOUT (154 bytes of zero slack), KEY_CUSTODY (~/.config/solana exists, no keypair), PYTH_PRICE_FEED_PROGRAM (HDwc = Wormhole bridge), HEALTH_URL (stalled since 2026-08-30T11:17Z), EXISTING_SITES (litepaper v0.7), RECEIPTS_API_URL (CORS allowlist wording). Wording fixes: events 8/9 not 8/8; instruction coverage 10/12; signer tallies 100>98; sources for RUST_VERSION and lemmalabs. New facts: DEVNET_SLOT_MS ~165 ms; USDC-d freeze authority = 2fpQ; deployed lib.rs = 56c719f0, main = c3a3b434 differs on OwnerWithdraw; IDL commit d26ebad2, snapshot in docs/idl/. Judge cargo-check evidence recorded on LITESVM_VERSION. Toolchain re-initialised to 4.2.2 afterwards.
```
